name: Prerelease

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Prerelease tag to create, for example v1.0.0-rc4'
        required: true
      npm_tag:
        description: 'Expected npm dist-tag used by release.yml for prereleases'
        required: true
        default: 'next'
      dry_run:
        description: 'Validate only; do not create tag or GitHub prerelease'
        required: true
        type: boolean
        default: true

permissions:
  actions: write
  contents: write

concurrency:
  group: prerelease-${{ inputs.version }}
  cancel-in-progress: false

jobs:
  prepare:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Validate prerelease input
        shell: bash
        run: |
          set -euo pipefail
          if [[ ! "${{ inputs.version }}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+-[0-9A-Za-z.-]+$ ]]; then
            echo "version must be a prerelease tag like v1.0.0-rc4" >&2
            exit 1
          fi
          if [[ "${{ inputs.npm_tag }}" != "next" ]]; then
            echo "Only npm dist-tag 'next' is supported for prerelease publishing." >&2
            exit 1
          fi

      - name: Resolve prerelease tag
        id: prerelease-tag
        shell: bash
        run: |
          set -euo pipefail
          git fetch --tags --force
          if git rev-parse -q --verify "refs/tags/${{ inputs.version }}" >/dev/null; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Tag ${{ inputs.version }} already exists; reusing it for release dispatch."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '24.15.0'

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate pure-JS package manifest
        run: node scripts/validate-prerelease.mjs "${{ inputs.version }}"

      - name: Validate docs snapshot
        run: pnpm docs:snapshot:release "${{ inputs.version }}" --dry-run

      - name: Build package
        run: pnpm --filter=stax-xml build

      - name: Validate pure-JS package contents
        run: node scripts/validate-prerelease.mjs "${{ inputs.version }}" --pack

      - name: Build docs
        run: pnpm docs:build
        env:
          NODE_ENV: production

      - name: Create prerelease tag
        if: ${{ !inputs.dry_run && steps.prerelease-tag.outputs.exists != 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -a "${{ inputs.version }}" -m "Prepare ${{ inputs.version }}"
          git push origin "${{ inputs.version }}"

      - name: Create GitHub prerelease
        if: ${{ !inputs.dry_run }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "${{ inputs.version }}" >/dev/null 2>&1; then
            echo "GitHub prerelease ${{ inputs.version }} already exists; skipping creation."
            exit 0
          fi
          gh release create "${{ inputs.version }}" \
            --title "${{ inputs.version }}" \
            --notes "Prerelease published through npm trusted publishing (OIDC). npm dist-tag: ${{ inputs.npm_tag }}." \
            --prerelease \
            --verify-tag

      - name: Dispatch release workflow
        if: ${{ !inputs.dry_run }}
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh workflow run release.yml --ref "${{ github.ref_name }}" -f version="${{ inputs.version }}"
