import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';
import test from 'node:test';

const repoRoot = resolve(import.meta.dirname, '..', '..');
const ledgerPath = resolve(repoRoot, 'docs', 'plans', '2026-05-23-stax-api-performance-proof-ledger.md');

function readLedger() {
  return readFileSync(ledgerPath, 'utf8');
}

function claimRow(markdown, id) {
  const row = markdown.split(/\r?\n/).find((line) => line.startsWith(`| \`${id}\` |`));
  assert.ok(row, `missing claim row: ${id}`);
  return row;
}

test('proof ledger keeps runtime-limit claims below conclusion strength', () => {
  const markdown = readLedger();
  assert.match(markdown, /## Proof Vocabulary/);
  assert.match(markdown, /## Runtime-Limit Conclusion Gate/);
  assert.match(markdown, /## Current Evidence: Runtime-Limit Proof Obligation Gate/);
  assert.match(markdown, /## Current Evidence: Same-Contract Runtime Comparison/);
  assert.match(markdown, /## Current Evidence: Runtime Counterexample Scan/);
  assert.match(markdown, /## Current Evidence: Runtime Proof Coverage Audit/);
  assert.match(markdown, /## Current Evidence: Runtime Proof Handoff Validation/);
  assert.match(markdown, /## Current Evidence: Safari\/WebKit Availability Audit/);
  assert.match(markdown, /## Current Evidence: Object-Shape Parity/);
  assert.match(markdown, /## Current Evidence: Materialization Contract Audit/);
  assert.match(markdown, /## Current Evidence: Woodstox HotSpot Trace/);
  assert.match(markdown, /## Current Evidence: Woodstox JFR Allocation Sampling/);
  assert.match(markdown, /## Current Evidence: Rust quick-xml Shape Audit/);
  assert.match(markdown, /## Current Evidence: Monomorphic Materialization Counters/);
  assert.match(markdown, /## Current Evidence: Candidate Headroom Matrix/);
  assert.match(markdown, /## Current Evidence: 1 GiB Candidate Headroom Matrix/);
  assert.match(markdown, /## Current Evidence: Bun\/JSC 1 GiB Candidate Headroom Matrix/);
  assert.match(markdown, /### Bun\/JSC CPU Profile/);
  assert.match(markdown, /## Current Evidence: Bun\/JSC 1 GiB Wide-Cycle Candidate Headroom Matrix/);
  assert.match(markdown, /## Current Evidence: 1 GiB Projection-Cycle Candidate Headroom Matrix/);
  assert.match(markdown, /## Current Evidence: Browser Chrome\/V8 1 GiB Projection-Cycle Candidate Headroom Matrix/);
  assert.match(markdown, /## Current Evidence: Firefox\/SpiderMonkey 1 GiB Projection-Cycle Candidate Headroom Matrix/);
  assert.match(markdown, /## Current Evidence: 1 GiB Corpus-Cycle Candidate Headroom Matrix/);
  assert.match(markdown, /## Current Evidence: Browser Chrome\/V8 1 GiB Candidate Headroom Matrix/);
  assert.match(markdown, /## Current Evidence: Firefox\/SpiderMonkey 1 GiB Candidate Headroom Matrix/);
  assert.match(markdown, /## Current Evidence: Firefox\/SpiderMonkey 1 GiB Corpus-Cycle Candidate Headroom Matrix/);
  assert.match(markdown, /## Current Evidence: Browser Chrome\/V8 1 GiB Corpus-Cycle Candidate Headroom Matrix/);
  assert.match(markdown, /## Current Evidence: Browser Chrome\/V8 Allocation Sampling/);
  assert.match(markdown, /## Current Evidence: EventReaderSync String-Input Large Reference/);
  assert.match(markdown, /## Current Evidence: V8 String Limit Audit/);
  assert.match(markdown, /## Current Evidence: Browser Chrome\/V8 String Limit Audit/);
  assert.match(markdown, /## Current Evidence: Chrome\/V8 Source Pin Audit/);
  assert.match(markdown, /## Current Evidence: Chrome\/Blink TextDecoder Source Pin Audit/);
  assert.match(markdown, /## Current Evidence: Firefox\/SpiderMonkey String Source Pin Audit/);
  assert.match(markdown, /## Current Evidence: Firefox\/SpiderMonkey TextDecoder Source Pin Audit/);
  assert.match(markdown, /## Current Evidence: Firefox\/SpiderMonkey Memory API Source Pin Audit/);
  assert.match(markdown, /## Current Evidence: Firefox\/SpiderMonkey Allocation Profile/);
  assert.match(markdown, /## Current Evidence: Firefox\/SpiderMonkey JS Shell StAX API Gap Audit/);
  assert.match(markdown, /## Current Evidence: Bun-Patched WebKit TextDecoder Source Pin Audit/);
  assert.match(markdown, /## Current Evidence: Bun TextDecoder Dispatch Source Pin Audit/);
  assert.match(markdown, /## Current Evidence: Bun\/JSC TextDecoder Codegen Trace/);
  assert.match(markdown, /## Current Evidence: Node TextDecoder Source Pin Audit/);
  assert.match(markdown, /## Current Evidence: Bun\/JSC String Limit Audit/);
  assert.match(markdown, /## Current Evidence: Bun\/JSC Source Pin Audit/);
  assert.match(markdown, /## Current Evidence: Bun\/JSC EventReaderSync String-Input Large Reference/);
  assert.match(markdown, /## Current Evidence: V8 Monomorphic Shape Trace/);
  assert.match(markdown, /## Current Evidence: V8 Allocation Sampling/);
  assert.match(markdown, /## Current Evidence: 1 GiB Generated Byte-Batch Shape/);
  assert.match(markdown, /## Current Evidence: TextDecoder Span Variant Matrix/);
  assert.match(markdown, /## Required Proof Tracks/);
  assert.match(markdown, /## Current Next Experiments/);

  assert.match(claimRow(markdown, 'CLAIM-JS-RUNTIME-LIMIT-200MIB'), /\| `HYPOTHESIS` \|/);
  assert.match(claimRow(markdown, 'CLAIM-WOODSTOX-SAME-JS-OBJECTS'), /\| `COUNTEREXAMPLE` \|/);
  assert.match(claimRow(markdown, 'CLAIM-QUICKXML-SAME-DATA'), /\| `SOURCE_FACT` \+ `BENCH_FACT` \+ partial `TRACE_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-QUICKXML-SAME-JS-OBJECTS'), /\| `COUNTEREXAMPLE` \|/);
  assert.match(claimRow(markdown, 'CLAIM-LAZY-GETTERS'), /\| `NEGATIVE_RESULT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-JS-STRING-ZERO-COPY'), /\| `ENGINE_INVARIANT`/);
  assert.match(claimRow(markdown, 'CLAIM-JS-STRING-MAX-LENGTH'), /\| `ENGINE_INVARIANT` \+ `SOURCE_FACT` \+ `TRACE_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-BUN-JSC-STRING-MAX-LENGTH'), /\| `ENGINE_INVARIANT` \+ `SOURCE_FACT` \+ `TRACE_FACT` \+ `COUNTEREXAMPLE` \|/);
  assert.match(claimRow(markdown, 'CLAIM-BUN-JSC-COMPLETE-STRING-1GIB'), /\| `BENCH_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-BUN-JSC-BYTE-BATCH-1GIB'), /\| `BENCH_FACT` \+ partial `TRACE_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-CORPUS-CYCLE-BYTE-BATCH-1GIB'), /\| `BENCH_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-BROWSER-V8-BYTE-BATCH-1GIB'), /\| `BENCH_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-FIREFOX-SPIDERMONKEY-BYTE-BATCH-1GIB'), /\| `BENCH_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-FIREFOX-SPIDERMONKEY-CORPUS-CYCLE-1GIB'), /\| `BENCH_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-FIREFOX-SPIDERMONKEY-PROJECTION-CYCLE-1GIB'), /\| `BENCH_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-BROWSER-V8-CORPUS-CYCLE-1GIB'), /\| `BENCH_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-BROWSER-V8-COMPLETE-STRING-1GIB'), /\| `BENCH_FACT` \+ `SOURCE_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-TEXTDECODER-SPAN-VARIANTS'), /\| `BENCH_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-NODE-TEXTDECODER-SOURCE-BOUNDARY'), /\| `SOURCE_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-CHROME-BLINK-TEXTDECODER-SOURCE-BOUNDARY'), /\| `SOURCE_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-BUN-WEBKIT-TEXTDECODER-SOURCE-BOUNDARY'), /\| `SOURCE_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-BUN-TEXTDECODER-DISPATCH-SOURCE-BOUNDARY'), /\| `SOURCE_FACT` \+ `COUNTEREXAMPLE` \|/);
  assert.match(claimRow(markdown, 'CLAIM-FIREFOX-SPIDERMONKEY-TEXTDECODER-SOURCE-BOUNDARY'), /\| `SOURCE_FACT` \|/);
  assert.match(claimRow(markdown, 'CLAIM-PROJECTION'), /\| `BENCH_FACT` for current low-selectivity rows; `HYPOTHESIS` for broad workloads \|/);

  assert.match(markdown, /may not move from `HYPOTHESIS` to\s+`CONCLUSION` merely because current full-string benchmark rows are slow/);
  assert.match(markdown, /A single engine, a\s+single generated cycle, or a single negative micro-variant is not sufficient/);
  assert.match(markdown, /Current allowed statement: the present artifacts have not found a\s+200 MiB\/s\+ bounded-memory full-string JavaScript counterexample/);
  assert.doesNotMatch(markdown, /JavaScript runtimes cannot exceed 200 MiB\/s/i);
  assert.match(markdown, /full-string parity candidate that reaches 200 MiB\/s\+ with bounded memory/);
  assert.match(markdown, /still headroom evidence for the narrower runtime\/parser boundary/);
  assert.match(markdown, /runtime-limit-proof-obligation-gate\.md/);
  assert.match(markdown, /incomplete-proof-correctly-blocked/);
  assert.match(markdown, /all 10 required claim guards are satisfied/);
  assert.match(markdown, /all 39 required artifact mentions\s+are present/);
  assert.match(markdown, /all 5 required open-obligation disclosures are present/);
  assert.match(markdown, /all 18\s+proof-rule checks are satisfied/);
  assert.match(markdown, /all 5\s+handoff-validation guards are satisfied/);
  assert.match(markdown, /`conclusionAllowed: false`/);
  assert.match(markdown, /treating Woodstox or quick-xml as JavaScript object\s+shape parity/);
  assert.match(markdown, /treating lazy getters as an untried default candidate/);
  assert.match(markdown, /turning JavaScript string\/runtime invariants or direct stream overhead into a\s+performance impossibility claim/);
  assert.match(markdown, /direct `ReadableStream`\s+overhead rows must remain distinct from synchronous byte-batch parser rows/);
  assert.match(markdown, /large byte-batch matrices must not be described as one\s+prebuilt 1 GiB\s+`ArrayBuffer` parser input/);
  assert.match(markdown, /byte-batch rows must preserve\s+demand-driven\s+pulls/);
  assert.match(markdown, /cross-fixture ratios\s+remain target-distance references/);
  assert.match(markdown, /same-fixture 1024 MiB JS\s+row is still below the 0\.9x Woodstox target even after\s+the file-backed source\s+and batch-size sweep rows are included/);
  assert.match(markdown, /requires the handoff\s+to carry the external Woodstox and quick-xml\s+target-distance evidence under the\s+same checksum contract/);
  assert.match(markdown, /future external\s+rows are judged against both the\s+200 MiB\/s counterexample threshold and the\s+0\.9x Woodstox goal/);
  assert.match(markdown, /requires the\s+text-materialization frontier to stay visible/);
  assert.match(markdown, /no-text\/CDATA\s+omission can cross\s+200 MiB\/s as headroom evidence, but it must not be counted\s+as a full-string StAX\s+counterexample/);
  assert.match(markdown, /If the broad\s+runtime-limit claim is upgraded to `CONCLUSION` while these open obligations\s+remain, the gate fails/);
  assert.match(markdown, /object-shape-parity\.md/);
  assert.match(markdown, /materialization-contract-audit\.md/);
  assert.match(markdown, /same-contract-runtime-comparison\.md/);
  assert.match(markdown, /runtime-counterexample-scan\.md/);
  assert.match(markdown, /runtime-proof-coverage-audit\.md/);
  assert.match(markdown, /source-consumption-shape-audit\.md/);
  assert.match(markdown, /memory-frontier-audit\.md/);
  assert.match(markdown, /target-distance-audit\.md/);
  assert.match(markdown, /text-materialization-boundary-audit\.md/);
  assert.match(markdown, /bun-jsc-partial-codegen-trace\.md/);
  assert.match(markdown, /bun-jsc-textdecoder-codegen-trace\.md/);
  assert.match(markdown, /full-string parity as\s+`not-applicable`, with zero full rows/);
  assert.match(markdown, /generated DFG JIT lines, 386 bytecode lines, 1,214 DFG node lines/);
  assert.match(markdown, /The traced rows preserved the full-string contract across\s+`subarraySharedDecoder`, `viewSharedDecoder`, `sliceCopySharedDecoder`,\s+`subarrayNewDecoder`, and `shortAsciiSubarraySharedDecoder`/);
  assert.match(markdown, /18\s+generated DFG JIT lines, 675 bytecode lines, 1,880 DFG node lines, and 59 target\s+mentions/);
  assert.match(markdown, /does\s+not prove generated native code inside Bun Zig `TextDecoder`/);
  assert.match(markdown, /289 aggregated rows and 239 JavaScript 1 GiB\+\s+full-string rows/);
  assert.match(markdown, /zero 200 MiB\/s\+ bounded-memory JavaScript\s+counterexamples/);
  assert.match(markdown, /current scan covers 217 primary release JSON artifacts/);
  assert.match(markdown, /fastest aggregated 1 GiB\+ JavaScript\s+full-string row is Node\/V8 `rawFrameNameId` from\s+`text-trim-cost-decomposition\.json` at 185\.50 MiB\/s/);
  assert.match(markdown, /184\.09 to 186\.66 MiB\/s/);
  assert.match(markdown, /fastest aggregate\s+row is `sync-iterable-byte-batches`/);
  assert.match(markdown, /fastest public event-object row is also\s+`sync-iterable-byte-batches`/);
  assert.match(markdown, /1024 MiB file-backed stax baseline is\s+`file-backed-sync-iterable-byte-batches`/);
  assert.match(markdown, /text-checksum consumer decomposition, and semantic checksum\s+upper-bound artifacts under the same checksum contract/);
  assert.match(markdown, /all 233 JavaScript 1 GiB\+ full-string\s+rows with source-mode metadata in this aggregate are marked as not full\s+`ArrayBuffer` parser-input rows/);
  assert.match(markdown, /separately records 150 corpus-seed replay\s+rows, with a maximum seed size of 100\.26 MiB and a maximum seed\/target ratio of\s+0\.09/);
  assert.match(markdown, /classifies memory on the same 239\s+JavaScript 1 GiB\+ full-string rows used by the counterexample scan: 222 rows\s+are bounded, 17 are unbounded or unproven/);
  assert.match(markdown, /metric kinds remain separate as\s+`process-rss`, `browser-js-heap`, and `browser-js-heap-unavailable`/);
  assert.match(markdown, /fastest bounded row is still Node\/V8 `rawFrameNameId` at 185\.50 MiB\/s with\s+process RSS max 60\.45 MiB/);
  assert.match(markdown, /fastest bounded browser JS heap row is Chrome\/V8\s+browser `rawFrameNameId` at 69\.90 MiB\/s with JS heap max 39\.55 MiB/);
  assert.match(markdown, /no-counter-materialization-candidate\.md/);
  assert.match(markdown, /`rawFrameNameIdNoCountersStringFoldCache`, reached 100\.43 MiB\/s with 83\.75 MiB\s+max RSS/);
  assert.match(markdown, /All\s+full rows preserved 57,096,514 events and checksum `-540013997`; no 200 MiB\/s\s+bounded-memory full-string JavaScript counterexample was found/);
  assert.match(markdown, /no-counter-materialization-batch1-candidate\.md/);
  assert.match(markdown, /`rawFrameNameIdNoCountersNameFoldCache` at 94\.71 MiB\/s with 73\.32 MiB max RSS/);
  assert.match(markdown, /batch-16 negative result was hiding a same-contract full-string win behind\s+multi-chunk grouping cost/);
  assert.match(markdown, /no-counter-materialization-batch1-cross-runtime-books-corpus\.md/);
  assert.match(markdown, /Bun\/JSC `rawFrameNameId` at 121\.23 MiB\/s, Node\/V8\s+`rawFrameNameId` at 114\.40 MiB\/s, and Deno\/V8 `rawFrameNameId` at\s+109\.47 MiB\/s/);
  assert.match(markdown, /prevents treating the\s+Node-only `batchSize=1` negative result as cross-runtime coverage/);
  assert.match(markdown, /Firefox\s+rows with unavailable page heap counters remain unbounded\/unproven rather than\s+being normalized through host-process probes/);
  assert.match(markdown, /name-collision-safe-interning-perf\.md/);
  assert.match(markdown, /`aSd3njyge` and `aSXRYquSd`/);
  assert.match(markdown, /`rawFrameNameId` at 96\.99 MiB\/s/);
  assert.match(markdown, /0\.93x of the 200 MiB\/s target and\s+0\.55x of the 1024 MiB Woodstox reference/);
  assert.match(markdown, /fastest aggregated JS row\s+and the 1024 MiB Woodstox reference can come from different corpus fixtures/);
  assert.match(markdown, /same-fixture 1024 MiB JS row vs Woodstox target now includes the\s+file-backed source, batch-size sweep, short attr-value cache, and trim-boundary\s+candidate rows/);
  assert.match(markdown, /`file-backed-trim-boundary-check-candidate\.json`, 351\.56 MiB\/s\), that is\s+0\.43x Woodstox and 164\.29 MiB\/s below the 0\.9x target/);
  assert.match(markdown, /This remains below the\s+0\.9x target/);
  assert.match(markdown, /same-fixture process RSS snapshot is now explicit but still\s+not an allocation-model equivalence claim/);
  assert.match(markdown, /fastest JS row records 61\.77\s+MiB max RSS, the selected Woodstox row records 312\.71 MiB max RSS, and the\s+selected quick-xml row records 4\.78 MiB max RSS/);
  assert.match(markdown, /For the\s+public event-object shape, the fastest bounded row is\s+Node\/V8 `eventObjectFull` from\s+`candidate-headroom-books-corpus-stability\.json` at 141\.62 MiB\/s/);
  assert.match(markdown, /141\.06 to 142\.03 MiB\/s/);
  assert.match(markdown, /16 MiB\s+quick-xml row is 243\.43 MiB\/s with process RSS max 4\.79 MiB, or 0\.80x\s+Woodstox/);
  assert.match(markdown, /not a peak-memory equivalence proof and not a runtime-limit conclusion/);
  assert.match(markdown, /recognizes 1,253\s+sample throughput rows and 182 aggregate rows/);
  assert.match(markdown, /Safari benchmark rows are not recorded, the exact Safari\/WebKit build identity\s+is not recorded, and the Safari\/WebKit source boundary is not pinned/);
  assert.match(markdown, /`Safari benchmark rows recorded: no`/);
  assert.match(markdown, /`Exact Safari build identity recorded: no`/);
  assert.match(markdown, /`Safari source boundary pinned:\s+no`/);
  assert.match(markdown, /`safari-webdriver-candidate-headroom\.mjs` and\s+`browser-candidate-headroom-cross-process\.mjs`/);
  assert.match(markdown, /848 JavaScript 1 GiB\+\s+full-string sample rows plus 142 JavaScript 1 GiB\+ full-string aggregate rows/);
  assert.match(markdown, /zero bounded-memory 200 MiB\/s\+ counterexamples/);
  assert.match(markdown, /zero measured\s+rows with unknown full-string parity and 23 rows with unknown bounded-memory\s+flags/);
  assert.match(markdown, /unknown bounded-memory set contains 7 JavaScript rows, 20\s+full-string rows, 0 JavaScript 1 GiB\+\s+full-string rows, and 10 rows that have\s+raw memory counters/);
  assert.match(markdown, /classifies this as 0 counterexample-relevant\s+unknown rows: 7 small\/diagnostic JavaScript rows, 10 non-JS allocator-counter\s+rows, and 6 non-JS trace or shape rows without peak-memory counters/);
  assert.match(markdown, /reports 91 full-string rows failing the\s+bounded-memory counterexample criterion/);
  assert.match(markdown, /91 carry explicit\s+`boundedMemory=false`/);
  assert.match(markdown, /0 have an unknown bounded-memory flag/);
  assert.match(markdown, /48\s+also lack row-level memory proof/);
  assert.match(markdown, /Node\/V8 `rawFrameNameId` from\s+`text-trim-cost-decomposition\.json` at 185\.50 MiB\/s/);
  assert.match(markdown, /fastest current cross-process aggregate full-string row with memory proof\s+is Node\/V8 `rawFrameNameId` from\s+`access-shape-candidate-cross-process-batch8\.json` at 172\.66 MiB\/s average over\s+3 samples with 3\.03% spread/);
  assert.match(markdown, /aggregate rows separately from\s+individual child samples/);
  assert.match(markdown, /474 JavaScript 1 GiB\+\s+full-string rows with source mode metadata/);
  assert.match(markdown, /generated-sync bucket now has\s+382 JavaScript 1 GiB\+ full-string rows, 373 of\s+them bounded/);
  assert.match(markdown, /file-backed sync byte-batch bucket adds 53 full-string rows,\s+52 of them bounded, with fastest row 152\.11 MiB\/s/);
  assert.match(markdown, /stable-shape event-object row\s+keeps the same file-backed `Iterable<Uint8Array\[\]>` source, 61,236,571 events,\s+checksum `-716099804`, and stable own-property shape/);
  assert.match(markdown, /it reaches 62\.32 MiB\/s with 133\.71 MiB\s+max RSS, only 1\.61 MiB\/s above the union public event-object row at\s+60\.71 MiB\/s and still below the public baseline at 85\.78 MiB\/s/);
  assert.match(markdown, /[Tt]he fastest\s+source-mode-classified row is Node\/V8 `rawFrameNameId` from\s+`text-trim-cost-decomposition\.json` at 185\.50 MiB\/s/);
  assert.match(markdown, /`complete-js-string`/);
  assert.match(markdown, /`sync-iterable-byte-batches` still covers four grouped sync rows after the focused\s+`stream-source-consumption-backpressure-counters\.json` audit/);
  assert.match(markdown, /`sync-iterable-byte-batches-batch-8`, which records parser input\s+`synchronous Iterable<Uint8Array\[\]>`, `directReadableStream=false`, and\s+71\.96 MiB\/s/);
  assert.match(markdown, /focused audit\s+now includes seven source-shape rows/);
  assert.match(markdown, /async `nextRawBatch\(\)` raw-frame\s+and short-ASCII raw-frame rows/);
  assert.match(markdown, /direct\s+`ReadableStream` `nextRawBatch\(\)` raw-frame and short-ASCII raw-frame rows/);
  assert.match(markdown, /`async-iterable-raw-frame-ascii-batch-8` at 77\.56 MiB\/s/);
  assert.match(markdown, /same backpressure counter contract rather than inferring it from the broader\s+source-shape matrix/);
  assert.match(markdown, /sync and async grouped-batch rows read 16,384 chunks plus one EOF\s+read and yield 2,048 `Uint8Array\[\]` batches/);
  assert.match(markdown, /direct\s+`ReadableStream` rows record 16,385 `pull\(\)` calls and 16,384 enqueues/);
  assert.match(markdown, /fastest backpressure-respecting `web-readable-stream-pull`\s+row is `web-readable-stream-raw-frame-ascii-batch-8`; it records parser input\s+`Web ReadableStream<Uint8Array>`, `directReadableStream=true`,\s+`respectsBackpressure=true`, and 76\.53 MiB\/s in the focused audit/);
  assert.match(markdown, /separates\s+parser-demand-driven source rows from direct ReadableStream rows and from Web\s+Stream backpressure rows/);
  assert.match(markdown, /`fetchReadableStreamFull`\s+is classified as `fetch-readable-stream-pull` with `directReadableStream=true`\s+and `respectsBackpressure=true` at 9\.68 MiB\/s/);
  assert.match(markdown, /`fetchAsyncByteBatchFull` is classified as `fetch-async-iterable-byte-batches`\s+with `directReadableStream=false` and `respectsBackpressure=true` at\s+9\.77 MiB\/s/);
  assert.match(markdown, /all 474\s+JavaScript 1 GiB\+ full-string rows with source-mode metadata are now marked as\s+not full `ArrayBuffer` parser-input rows/);
  assert.match(markdown, /fixes the previous scanner\s+blind spot/);
  assert.match(markdown, /non-`stax-\*` Node\/V8 row tools could be labeled `Node\/V8` but\s+not counted as JavaScript runtime rows/);
  assert.match(markdown, /remain below the 200 MiB\/s\s+counterexample threshold/);
  assert.match(markdown, /35 threshold-crossing partial\/projection rows/);
  assert.match(markdown, /fastest is Node\/V8 `grouped-segment-scan` at 682\.83 MiB\/s from\s+`packages\/benchmark\/results\/release\/segment-scan-headroom\.md`/);
  assert.match(markdown, /records\s+`directReadableStream=false`, and is explicitly `fullStringParity=false`/);
  assert.match(markdown, /grouped segment-aware scan at 682\.83 MiB\/s versus grouped\s+concat-before-scan at 589\.23 MiB\/s, or 1\.16x/);
  assert.match(markdown, /matching delimiter count\s+162,096,810 and checksum `-428951732`/);
  assert.match(markdown, /falsifiable no-concat\s+parser-core headroom result, not an XML parser and not a full StAX\s+counterexample/);
  assert.match(markdown, /`segment-tokenizer-headroom\.json` is also\s+counted as Node\/V8 benchmark and scope-guard evidence/);
  assert.match(markdown, /singleton segment row reaches 236\.55 MiB\/s with 62\.69 MiB max RSS/);
  assert.match(markdown, /grouped segment-aware row reaches only 196\.26 MiB\/s versus grouped concat\s+at 194\.55 MiB\/s, or 1\.01x/);
  assert.match(markdown, /matching 61,236,569 events, 21,612,907\s+start elements, 21,612,907 end elements, 18,010,755 text events,\s+18,010,755 attributes, and checksum `-1381363934`/);
  assert.match(markdown, /1\.16x delimiter-scan no-concat headroom mostly disappears once token-boundary\s+work is included/);
  assert.match(markdown, /`segment-tokenizer-string-frontier\.json` adds\s+the next partial frontier: grouped segment tokenization plus `TextDecoder`\s+materialization/);
  assert.match(markdown, /Its `tokenOnly` row\s+reached 234\.30 MiB\/s, but element-name strings alone drop to 111\.32 MiB\/s after\s+43,225,814 decode calls and\s+288,172,088 decoded bytes/);
  assert.match(markdown, /name string cache cuts the\s+element-name row to 6 decode calls and raises it to 153\.14 MiB\/s with the same\s+checksum/);
  assert.match(markdown, /all token strings without public objects still reach only\s+66\.58 MiB\/s after 97,258,079 decode calls and 882,827,595 decoded bytes/);
  assert.match(markdown, /name-cached all-token row reaches 81\.14 MiB\/s after 36,021,520 decode calls,\s+61,236,559 cache hits, 10 cached names, and 515,408,238 decoded bytes/);
  assert.match(markdown, /bounded all-string cache with 4,096 retained entries improves the element plus\s+attribute string row from 83\.34 to 112\.27 MiB\/s by reducing decode calls to\s+3,602,259, but the same bounded cache on all token strings reaches only\s+74\.85 MiB\/s/);
  assert.match(markdown, /segment-tokenizer-full-checksum-candidate\.json/);
  assert.match(markdown, /tests three full-checksum segmented candidates: no\s+cache, name cache, and bounded all-string cache/);
  assert.match(markdown, /All preserve 61,236,571\s+events and checksum `-716099804`/);
  assert.match(markdown, /The fastest is\s+`allTokenStringsNameCachedDocumentEventsNoObjects` at 52\.73 MiB\/s with\s+97\.78 MiB max RSS after 36,021,520 decode calls and 61,236,559 cache hits/);
  assert.match(markdown, /bounded all-string cache reduces decode calls further to 21,028,479 but drops\s+to 46\.27 MiB\/s/);
  assert.match(markdown, /repeated names and some\s+attribute values recover decode cost, while text string materialization still\s+dominates before public event-object allocation is even reintroduced/);
  assert.match(markdown, /Bun\/JSC\s+`scanAllNoDecode` at 326\.65 MiB\/s from\s+`candidate-headroom-cross-process-books-corpus-partial\.json` remains the\s+fastest parser-produced partial row/);
  assert.match(markdown, /The fastest is Node\/V8 `withoutTextStrings` from\s+`text-trim-cost-decomposition-4gib\.json` at 252\.36 MiB\/s/);
  assert.match(markdown, /`rawFrameNameIdNoTrim` at 185\.03 MiB\/s\s+with the same 62,758,976 string-field reads/);
  assert.match(markdown, /`rawFrameNameIdLongTextCache` row at 141\.85 MiB\/s despite 4,482,765\s+cache hits and only 19 misses/);
  assert.match(markdown, /bounded long-text cache at 0\.83x of its control/);
  assert.match(markdown, /`offset-text-cache-materialization-candidate\.json` tests whether the repeated\s+`books\.xml` corpus-cycle can use a cheaper buffer-identity plus span-offset\s+cache/);
  assert.match(markdown, /records zero cache hits, 16,987,392 misses, 105\.41 MiB\/s, and\s+198\.4 MiB max RSS/);
  assert.match(markdown, /raw-frame buffer identity is not stable enough for\s+that cache key/);
  assert.match(markdown, /`medium-ascii-text-materialization-candidate\.json` artifact tests a narrower\s+manual string path for 13-24 byte ASCII text\/CDATA spans/);
  assert.match(markdown, /improves its same-run control from 164\.31 MiB\/s to 170\.16 MiB\/s/);
  assert.match(markdown, /after 2,831,232\s+medium-ASCII hits and 3,539,040 fallbacks/);
  assert.match(markdown, /`unrolled-medium-ascii-text-materialization-candidate\.json` artifact then\s+removes the split 12-byte-plus-tail string concatenation/);
  assert.match(markdown, /improves its same-run control from\s+164\.13 MiB\/s to 170\.59 MiB\/s/);
  assert.match(markdown, /after 2,831,232 unrolled medium-ASCII hits and\s+3,539,040 fallbacks/);
  assert.match(markdown, /`unrolled-medium-ascii-text-trim-guard-candidate\.json` artifact combines that\s+unrolled medium-ASCII text path with the byte-boundary text trim guard/);
  assert.match(markdown, /improves its\s+same-run control from 136\.23 MiB\/s to 164\.14 MiB\/s/);
  assert.match(markdown, /slower than the\s+same-run unrolled-only row at 166\.33 MiB\/s/);
  assert.match(markdown, /16,987,392 trim-guard skips, and zero trim-guard\s+fallbacks/);
  assert.match(markdown, /`medium-ascii-attr-value-materialization-candidate\.json` artifact applies the\s+same 13-24 byte ASCII split strategy to attribute values/);
  assert.match(markdown, /improves its same-run control only\s+from 165\.38 MiB\/s to 167\.49 MiB\/s/);
  assert.match(markdown, /2,831,232 raw attribute-value span materializations but zero medium-ASCII\s+attribute-value hits or fallbacks/);
  assert.match(markdown, /`attr-value-cache-materialization-candidate\.json` then narrows value caching to\s+only attribute values/);
  assert.match(markdown, /2,831,220 cache hits, 12 misses, and only 12 raw attribute-value span\s+materializations/);
  assert.match(markdown, /slows from its same-run control at 161\.24 MiB\/s to\s+158\.96 MiB\/s/);
  assert.match(markdown, /`withoutAttributeValueStrings` row reports 151\.93 MiB\/s and\s+checksum `1597287507`/);
  assert.match(markdown, /`bun-cache-candidates-books-corpus\.json` repeats the cache and unrolled-text\s+negative candidates under Bun\/JSC/);
  assert.match(markdown, /Bun\/JSC\s+`rawFrameNameId` reports 169\.07 MiB\/s with checksum `-540013997`/);
  assert.match(markdown, /attribute-value cache row reports 158\.09 MiB\/s despite 2,831,220 cache hits and\s+12 misses/);
  assert.match(markdown, /buffer-offset text cache row reports 99\.92 MiB\/s with zero hits\s+and 16,987,392 misses/);
  assert.match(markdown, /unrolled medium-ASCII text row reports\s+150\.27 MiB\/s after the same 2,831,232 hits and 3,539,040 fallbacks/);
  assert.match(markdown, /`withoutTextStrings` at\s+213\.15 MiB\/s, but it changes the checksum to `1372281363`/);
  assert.match(markdown, /`raw-span-shape-audit\.json` records the corresponding source\/span distribution\s+without adding a throughput row/);
  assert.match(markdown, /57,096,514 events and 62,758,976 materialized string spans/);
  assert.match(markdown, /2,831,232 13-24 byte ASCII text spans and 3,539,040 >24 byte ASCII text spans/);
  assert.match(markdown, /all 2,831,232 explicit attribute-value spans are 5-byte ASCII and zero\s+explicit attribute values fall in the 13-24 byte bucket/);
  assert.match(markdown, /omitted all\s+16,987,392 text\/CDATA string reads and changed the checksum to `1372281363`/);
  assert.match(markdown, /text-materialization-frontier\.md/);
  assert.match(markdown, /fastest full row is\s+still 185\.50 MiB\/s, 14\.50 MiB\/s below the 200 MiB\/s counterexample threshold/);
  assert.match(markdown, /1\.08x full-string speedup is still required/);
  assert.match(markdown, /maximum\s+same-scale without-text speedup is 1\.41x at 4 GiB after omitting 67,949,424\s+text string reads/);
  assert.match(markdown, /maximum no-trim speedup at only\s+1\.02x, maximum fold-trim speedup at 0\.80x/);
  assert.match(markdown, /repeated text-value cache at 0\.74x of\s+its control, buffer-offset text-value cache at 0\.66x of its control, bounded long-text cache at 0\.83x of its control, long ASCII text\s+fast path at 0\.41x of its control, medium ASCII text fast path at 1\.04x of its\s+control, unrolled medium ASCII text fast path at 1\.04x of its control,\s+unrolled medium ASCII text plus trim guard at 1\.20x of its same-run control,\s+fold-trim checksum at 0\.84x of its control, the byte-boundary trim guard at\s+1\.00x of its control, ASCII byte pre-trim before decode at 0\.99x of its control,\s+manual ASCII materialization for all string spans at 0\.63x of its control, and\s+direct semantic byte checksum at 0\.97x of its same-condition control/);
  assert.match(markdown, /batch-size-1 cross-runtime no-counter\s+fold-cache rows: Node\/V8, Bun\/JSC, and Deno\/V8 all preserve full-string parity,\s+but both name-fold and string-fold cache candidates remain below their\s+`rawFrameNameId` controls and below 200 MiB\/s/);
  assert.match(markdown, /text-folding-cost-candidate\.md/);
  assert.match(markdown, /rawFrameNameIdTextNoFold` kept the same 62,758,976 string-field reads and\s+16,987,392 text string reads/);
  assert.match(markdown, /it reached only 162\.70 MiB\/s, or 1\.08x its\s+control, still below 200 MiB\/s/);
  assert.match(markdown, /rawFrameNameIdTextLengthOnly` also kept the\s+text string reads while folding only text lengths/);
  assert.match(markdown, /reached 155\.89 MiB\/s, or 1\.04x control/);
  assert.match(markdown, /withoutTextStrings` control reached 160\.29 MiB\/s with zero text string reads/);
  assert.match(markdown, /negative evidence against\s+text checksum folding alone explaining the remaining gap/);
  assert.match(markdown, /text-trim-guard-candidate\.md/);
  assert.match(markdown, /`rawFrameNameId`\s+averaged 109\.66 MiB\/s and `rawFrameNameIdTrimGuard` averaged 109\.56 MiB\/s/);
  assert.match(markdown, /skipped `value\.trim\(\)` for 11,950,239 text\/CDATA spans and fell back 374,103\s+times/);
  assert.match(markdown, /text-ascii-pretrim-candidate\.md/);
  assert.match(markdown, /`rawFrameNameId`\s+averaged 107\.10 MiB\/s and `rawFrameNameIdAsciiPreTrim`\s+averaged 106\.35 MiB\/s/);
  assert.match(markdown, /it skipped 11,950,239 spans and fell back 374,103 times/);
  assert.match(markdown, /all-ascii-span-materialization-candidate\.md/);
  assert.match(markdown, /`rawFrameNameId` averaged\s+109\.64 MiB\/s while `rawFrameNameIdAllAsciiSpans` averaged only 68\.63 MiB\/s/);
  assert.match(markdown, /despite 11,840,912 ASCII-span hits and 374,103 fallbacks/);
  assert.match(markdown, /max RSS also rose\s+from 77\.8 MiB to 94\.9 MiB/);
  assert.match(markdown, /sync-byte-batch-shape-batch1\.md/);
  assert.match(markdown, /sync-byte-batch-shape-batch16\.md/);
  assert.match(markdown, /batchSize=1 recorded `rawFrameNameId` at 102\.21 MiB\/s, `stringFull` at\s+92\.23 MiB\/s, and public `eventObjectFull` at 74\.26 MiB\/s/);
  assert.match(markdown, /batchSize=16\s+recorded `rawFrameNameId` at 108\.21 MiB\/s, `stringFull` at 97\.81 MiB\/s, and\s+public `eventObjectFull` at 71\.90 MiB\/s/);
  assert.match(markdown, /remaining synchronous input overhead is a balance among parser pull frequency,\s+multi-chunk concat copy, and materialization\/object allocation/);
  assert.match(markdown, /throughput `41\.10 MiB\/s`, peak heap used `4\.00 GiB`, and peak RSS\s+`13\.45 GiB`/);
  assert.match(markdown, /`sourceMode: "complete-js-string"` and\s+`boundedMemory: false` under the 512 MiB RSS gate/);
  assert.match(markdown, /headroom evidence rather than runtime-limit\s+counterexamples/);
  assert.match(markdown, /recognizes 1,253\s+measured rows/);
  assert.match(markdown, /current audit scans 217 primary release artifacts/);
  assert.match(markdown, /155 benchmark artifacts, 24 source artifacts, 15\s+trace\/profile artifacts, 16 allocation artifacts, 4 environment artifacts, and\s+24 negative-result artifacts, 848 JavaScript 1 GiB\+ full-string rows/);
  assert.match(markdown, /runtime-proof-handoff-validation\.md/);
  assert.match(markdown, /statically validates the two external-run handoffs/);
  assert.match(markdown, /12\s+referenced scripts exist, 28 release output paths are under the curated release\s+directory/);
  assert.match(markdown, /zero missing scripts or output-path policy violations/);
  assert.match(markdown, /synchronous `Iterable<Uint8Array\[\]>` byte batches are the\s+primary parser input/);
  assert.match(markdown, /does not emit SpiderMonkey IR or optimized code/);
  assert.match(markdown, /spidermonkey-jsshell-diagnostic-flag-sweep\.md/);
  assert.match(markdown, /four public-shell bytecode flag combinations/);
  assert.match(markdown, /zero\s+bytecode-output probes, and `diagnosticPrefSurface=false`/);
  assert.match(markdown, /spidermonkey-taskcluster-debug-jsshell-codegen-audit\.md/);
  assert.match(markdown, /Taskcluster `mozilla-central` win64-debug/);
  assert.match(markdown, /task `bzK0wWZvQoOguMjTIbRJ_g`/);
  assert.match(markdown, /build id `20260531212007`/);
  assert.match(markdown, /source revision `71e37c8757f87e7682d7db7d9b9ec9f7f81e24f7`/);
  assert.match(markdown, /54,756\s+`\[Codegen\]` markers, 5 IonScript mentions, and 20,931 assembly mnemonic/);
  assert.match(markdown, /`sameContractStaxRow=false`/);
  assert.match(markdown, /`canRunCurrentStaxFullStringBenchmark=false`/);
  assert.match(markdown, /current-debug-codegen-scope-guard/);
  assert.match(markdown, /spidermonkey-taskcluster-debug-jsshell-xml-codegen-audit\.md/);
  assert.match(markdown, /XML\s+byte-tokenizer workload under `IONFLAGS=codegen` and `JIT_SPEW=codegen`/);
  assert.match(markdown, /151,431\s+`\[Codegen\]` markers, 49\s+IonScript mentions, and 55,580 assembly mnemonic/);
  assert.match(markdown, /55,759 token-boundary events and checksum `9292058`/);
  assert.match(markdown, /`current-debug-xml-codegen-scope-guard`/);
  assert.match(markdown, /spidermonkey-taskcluster-debug-jsshell-materialized-codegen-audit\.md/);
  assert.match(markdown, /ASCII XML workload that materializes JavaScript\s+string primitives and public event-shaped objects/);
  assert.match(markdown, /234,522\s+`\[Codegen\]` markers, 60 IonScript mentions, and 86,190 assembly mnemonic/);
  assert.match(markdown, /223,041 materialized events, checksum `167904020`,\s+245,161 materialized strings, and 223,041 public event-shaped objects/);
  assert.match(markdown, /spidermonkey-materialized-scope-distance-audit\.md/);
  assert.match(markdown, /semanticEquivalentForAsciiFields=true/);
  assert.match(markdown, /spidermonkey-ascii-scope-distance-audit\.md/);
  assert.match(markdown, /All corpus files ASCII-only: yes/);
  assert.match(markdown, /ASCII byte materializer equals UTF-8 TextDecoder for checked ASCII spans: yes/);
  assert.match(markdown, /closesCodegenObligation=false/);
  assert.match(markdown, /`current-debug-materialized-codegen-scope-guard`/);
  assert.match(markdown, /spidermonkey-archival-debug-jsshell-codegen-audit\.md/);
  assert.match(markdown, /Firefox 36 era debug js-shell/);
  assert.match(markdown, /archival-codegen-scope-guard/);
  assert.match(markdown, /does not close `codegen-traces-open`/);
  assert.match(markdown, /concat-buffer-reuse-negative-result\.json/);
  assert.match(markdown, /stax-raw-frame-span-stats/);
  assert.match(markdown, /segment-scan-headroom\.json/);
  assert.match(markdown, /benchmark-only delimiter byte-scan probe for the no-concat parser-core axis/);
  assert.match(markdown, /rows are not full-string parity rows and therefore do not change the 1 GiB\+\s+full-string counterexample search/);
  assert.match(markdown, /segment-tokenizer-headroom\.json/);
  assert.match(markdown, /folds start\/end\/text\s+token boundaries and attribute counts without JavaScript string materialization/);
  assert.match(markdown, /showing 236\.55 MiB\/s for singleton segment pulls but only 196\.26 MiB\/s for the\s+grouped segment-aware row/);
  assert.match(markdown, /segment-tokenizer-string-frontier\.json/);
  assert.match(markdown, /grouped segment tokenization plus `TextDecoder`\s+materialization/);
  assert.match(markdown, /token-only 234\.30 MiB\/s, element-name strings\s+111\.32 MiB\/s, cached element-name strings 153\.14 MiB\/s, all token strings\s+without public objects 66\.58 MiB\/s, name-cached all token strings\s+81\.14 MiB\/s, and bounded all-string cache all token strings 74\.85 MiB\/s/);
  assert.match(markdown, /full-checksum follow-up rows: no-cache, name-cache, and bounded all-string-cache\s+variants all match the `StreamReaderSync` reference event count and checksum on\s+the same 1024 MiB fixture, but the fastest reaches only 52\.73 MiB\/s with\s+97\.78 MiB max RSS/);
  assert.match(markdown, /four\s+release corpus seeds:\s+`books\.xml`, `large\.xml`, `midsize\.xml`, and `treebank_e\.xml`/);
  assert.match(markdown, /coverage audit now treats `runtime\.id: "node"` \/ `runtime\.v8` artifacts such as\s+`stream-reader-4gb-shapes\.json` as Node\/V8 row evidence/);
  assert.match(markdown, /rather than leaving\s+their 4 GiB generated byte-batch rows in the `unknown` bucket/);
  assert.match(markdown, /normalize `events` as row event count for those rows/);
  assert.match(markdown, /4 GiB generated byte-batch parity evidence now records 572,662,314 events and\s+checksum `-1788666544`/);
  assert.match(markdown, /removing the remaining `unknown` runtime\s+coverage bucket/);
  assert.match(markdown, /`node-textdecoder-source-pin-audit\.json` and\s+`projection-benchmark\.json`/);
  assert.match(markdown, /Derived `summary\.fastest` \/ `summary\.slowest` projections and `comparisons`\s+views are excluded from row extraction/);
  assert.match(markdown, /source-consumption and runtime-matrix\s+rows are not counted twice/);
  assert.match(markdown, /98 Chrome\/V8 browser rows, 82 Firefox\/SpiderMonkey\s+browser rows, zero Safari\/WebKit browser rows, and 82 non-V8 browser benchmark\s+rows/);
  assert.match(markdown, /Bun\/JSC and\s+Bun-patched WebKit evidence is not Safari\/browser\s+JSC evidence/);
  assert.match(markdown, /safari-webkit-availability-audit\.md/);
  assert.match(markdown, /Current harness supports Safari\/WebKit: yes/);
  assert.match(markdown, /safaridriver WebDriver wrapper/);
  assert.match(markdown, /cross-process stability runner/);
  assert.match(markdown, /not a benchmark row/);
  assert.match(markdown, /Safari\/WebKit cannot be a counterexample/);
  assert.match(markdown, /2 open or partial obligations/);
  assert.match(markdown, /runtime-proof-gap-handoff\.md/);
  assert.match(markdown, /safari-webkit-browser-row-handoff/);
  assert.match(markdown, /spidermonkey-codegen-handoff/);
  assert.match(markdown, /source-consumption evidence status is\s+`classified`/);
  assert.match(markdown, /source modes\s+`fetch-async-iterable-byte-batches`, `fetch-readable-stream-pull`,\s+`file-backed-sync-iterable-byte-batches`, and `sync-iterable-byte-batches`/);
  assert.match(markdown, /all\s+233 JavaScript 1 GiB\+ full-string rows with source-mode metadata are not full\s+`ArrayBuffer` parser-input rows/);
  assert.match(markdown, /`sync-iterable-byte-batches-batch-8` at 71\.96 MiB\/s versus\s+`web-readable-stream-raw-frame-ascii-batch-8` at 76\.53 MiB\/s/);
  assert.match(markdown, /browser live fetch frontier records `fetchReadableStreamFull` at 9\.68\s+MiB\/s and `fetchAsyncByteBatchFull` at 9\.77 MiB\/s with backpressure 2\/2/);
  assert.match(markdown, /classification closes only the source-consumption guard; Safari\/WebKit browser\s+rows and SpiderMonkey emitted IR remain active obligations/);
  assert.match(markdown, /same-contract memory frontier from that\s+aggregate: 239 JavaScript 1 GiB\+ full-string memory rows, 222 bounded rows, and\s+17 unbounded or unproven rows/);
  assert.match(markdown, /across `process-rss`, `browser-js-heap`, and\s+`browser-js-heap-unavailable` memory kinds/);
  assert.match(markdown, /fastest bounded row is Node\/V8\s+`rawFrameNameId` at 185\.50 MiB\/s with 60\.45 MiB process RSS/);
  assert.match(markdown, /fastest browser\s+JS heap row is Chrome\/V8 browser `rawFrameNameId` at 69\.90 MiB\/s with 39\.55 MiB\s+JS heap/);
  assert.match(markdown, /Firefox\/SpiderMonkey rows whose page heap counters are unavailable\s+remain classified as unbounded or unproven rather than being normalized through\s+Windows host process-tree probes/);
  assert.match(markdown, /external target-distance evidence from the same\s+aggregate/);
  assert.match(markdown, /fastest aggregated\s+JavaScript full-string row is 0\.93x of the 200 MiB\/s threshold and 0\.55x of the\s+1024 MiB Woodstox reference/);
  assert.match(markdown, /118\.67 MiB\/s below the 0\.9x Woodstox target/);
  assert.match(markdown, /`stax-raw-frame-name-id-batch-8` reaches 152\.11 MiB\/s; Woodstox is 351\.56 MiB\/s\s+with a 316\.40 MiB\/s 0\.9x target, and quick-xml is 274\.63 MiB\/s with a\s+247\.17 MiB\/s 0\.9x target/);
  assert.match(markdown, /same-fixture process RSS snapshot remains\s+separate endpoint evidence: JavaScript 61\.77 MiB, Woodstox 312\.71 MiB, and\s+quick-xml 4\.78 MiB/);
  assert.match(markdown, /target-distance evidence under the same checksum\s+contract, not object-shape equivalence/);
  assert.match(markdown, /handoff also carries the text-materialization frontier/);
  assert.match(markdown, /fastest full-string row remains\s+`rawFrameNameId` from `text-trim-cost-decomposition\.json` at 185\.50 MiB\/s,\s+14\.50 MiB\/s below the 200 MiB\/s threshold and requiring a 1\.08x speedup/);
  assert.match(markdown, /`withoutTextStrings` from\s+`text-trim-cost-decomposition-4gib\.json`, reaches 252\.36 MiB\/s with bounded\s+memory, but it is not full-string parity/);
  assert.match(markdown, /four no-text rows cross 200 MiB\/s\s+while zero full-string, no-trim, or fold-trim rows do/);
  assert.match(markdown, /without reclassifying text omission as\s+a StAX-compatible full-string row/);
  assert.match(markdown, /no unhandled gaps/);
  assert.match(markdown, /structured `localClosure` status/);
  assert.match(markdown, /Safari is\s+`external-run-required` with `localRunnable=false`/);
  assert.match(markdown, /Firefox\/SpiderMonkey codegen is also `external-run-required` with\s+`localRunnable=false`/);
  assert.match(markdown, /installed Firefox diagnostic audit emitted no\s+JIT diagnostic dump, the installed Firefox buildconfig does not expose the\s+JitSpew build flag, and the available release\/nightly SpiderMonkey JS shells do\s+not expose the unchanged StAX benchmark host API surface or an emitted\s+IR\/codegen dump surface/);
  assert.match(markdown, /official release jsshell artifact proves a\s+runnable release shell and Ion status probe, but also records no JitSpew\/IR\s+dump flag surface/i);
  assert.match(markdown, /IONFLAGS=logs,codegen,mir,lir,aborts,scripts` and `JIT_SPEW` set to the same\s+value still reports `no-jitspew-output`, zero diagnostic markers, and zero\s+stderr lines/);
  assert.match(markdown, /firefox-spidermonkey-jsshell-stax-api-gap-audit\.md/);
  assert.match(markdown, /`status=blocked-by-host-api-surface`, two available shells, two shells with JIT\s+status probes, two shells that can read binary XML input, and zero shells that\s+can run the current full-string StAX benchmark unchanged/);
  assert.match(markdown, /Both shells have `Uint8Array`, but both lack `TextDecoder`, `TextEncoder`,\s+`ReadableStream`, and `fetch`/);
  assert.match(markdown, /Adding a polyfill or alternate decoder\s+would create a different harness surface/);
  assert.match(markdown, /no active disassembler, and an\s+incomplete `disnative` byte\s+dump/);
  assert.match(markdown, /candidate-headroom-cross-process-books-corpus-batch16\.md/);
  assert.match(markdown, /multi-item batch is\s+concatenated into one parser buffer/);
  assert.match(markdown, /Node\/V8 `rawFrameNameId` averaged `99\.83 MiB\/s`/);
  assert.match(markdown, /Bun\/JSC `rawFrameNameId` averaged `92\.49 MiB\/s`/);
  assert.match(markdown, /file-backed-batch-size-sweep\.md/);
  assert.match(markdown, /from batch sizes 1, 2, 4, 8, and 16 to include 32 and 64/);
  assert.match(markdown, /`stax-raw-frame-name-id-batch-8` at `152\.11 MiB\/s` with `61\.77 MiB` max RSS/);
  assert.match(markdown, /`stax-raw-frame-name-id-batch-64` reported `148\.68 MiB\/s` with `87\.03 MiB`\s+RSS/);
  assert.match(markdown, /This keeps the source-shape evidence on the sync byte-batch path/);
  assert.match(markdown, /multi-chunk-batch-shape-audit\.md/);
  assert.match(markdown, /single-buffer span\s+model/);
  assert.match(markdown, /segmented-buffer abstraction/);
  assert.match(markdown, /does not\s+turn missing evidence\s+into\s+evidence that optimization\s+is impossible/);
  assert.match(markdown, /same semantic fields/);
  assert.match(markdown, /not the same object shape/);
  assert.match(markdown, /Woodstox uses `XMLStreamReader` cursor\/accessor calls/);
  assert.match(markdown, /683,270 element local-name reads/);
  assert.match(markdown, /1,537,355 folded strings/);
  assert.match(markdown, /Lazy getters\s+remain a recorded negative result/);
  assert.match(markdown, /woodstox-hotspot-trace\.md/);
  assert.match(markdown, /woodstox-jfr-allocation\.md/);
  assert.match(markdown, /woodstox-measured-jfr-allocation\.md/);
  assert.match(markdown, /woodstox-measured-jfr-allocation-rerun\.md/);
  assert.match(markdown, /quick-xml-shape-audit\.md/);
  assert.match(markdown, /quick-xml-allocation-count\.md/);
  assert.match(markdown, /quick-xml-allocation-count-stability\.md/);
  assert.match(markdown, /quick-xml-encoding-surface-audit\.md/);
  assert.match(markdown, /monomorphic-batch-access\.md/);
  assert.match(markdown, /candidate-headroom-matrix\.md/);
  assert.match(markdown, /candidate-headroom-large\.md/);
  assert.match(markdown, /candidate-headroom-large-stability\.md/);
  assert.match(markdown, /bun-candidate-headroom-large\.md/);
  assert.match(markdown, /bun-candidate-headroom-large-stability\.md/);
  assert.match(markdown, /bun-jsc-cpu-profile\.md/);
  assert.match(markdown, /bun-jsc-memory-allocation-profile\.md/);
  assert.match(markdown, /bun-jsc-heap-allocation-profile\.md/);
  assert.match(markdown, /bun-candidate-headroom-wide\.md/);
  assert.match(markdown, /candidate-headroom-projection-large\.md/);
  assert.match(markdown, /bun-candidate-headroom-projection-large\.md/);
  assert.match(markdown, /candidate-headroom-projection-stability\.md/);
  assert.match(markdown, /bun-candidate-headroom-projection-stability\.md/);
  assert.match(markdown, /candidate-headroom-cross-process-projection\.md/);
  assert.match(markdown, /browser-candidate-headroom-projection-large\.md/);
  assert.match(markdown, /browser-candidate-headroom-projection-stability\.md/);
  assert.match(markdown, /browser-candidate-headroom-cross-process-projection\.md/);
  assert.match(markdown, /candidate-headroom-corpus\.md/);
  assert.match(markdown, /bun-candidate-headroom-corpus\.md/);
  assert.match(markdown, /candidate-headroom-books-corpus\.md/);
  assert.match(markdown, /bun-candidate-headroom-books-corpus\.md/);
  assert.match(markdown, /candidate-headroom-books-corpus-stability\.md/);
  assert.match(markdown, /bun-candidate-headroom-books-corpus-stability\.md/);
  assert.match(markdown, /candidate-headroom-cross-process-books-corpus\.md/);
  assert.match(markdown, /candidate-headroom-cross-process-large-asset-corpus\.md/);
  assert.match(markdown, /browser-candidate-headroom-cross-process-books-corpus\.md/);
  assert.match(markdown, /firefox-bidi-candidate-headroom-cross-process-books-corpus\.md/);
  assert.match(markdown, /browser-candidate-headroom-books-corpus\.md/);
  assert.match(markdown, /firefox-bidi-candidate-headroom-books-corpus\.md/);
  assert.match(markdown, /browser-candidate-headroom-large\.md/);
  assert.match(markdown, /firefox-bidi-candidate-headroom\.md/);
  assert.match(markdown, /firefox-bidi-candidate-headroom-projection\.md/);
  assert.match(markdown, /firefox-bidi-candidate-headroom-cross-process-projection\.md/);
  assert.match(markdown, /firefox-bidi-candidate-headroom-corpus\.md/);
  assert.match(markdown, /browser-candidate-headroom-corpus\.md/);
  assert.match(markdown, /browser-v8-allocation-sampling\.md/);
  assert.match(markdown, /browser-v8-codegen-trace\.md/);
  assert.match(markdown, /bun-jsc-codegen-trace\.md/);
  assert.match(markdown, /Bun\/JSC Codegen Trace/);
  assert.match(markdown, /JSC_dumpBytecodeAtDFGTime=true/);
  assert.match(markdown, /15 generated DFG JIT lines/);
  assert.match(markdown, /47,753 events and checksum\s+`-1316984100`/);
  assert.match(markdown, /Browser Chrome\/V8 Codegen Trace/);
  assert.match(markdown, /V8 optimization status availability/);
  assert.match(markdown, /318,974 events and\s+checksum `1006682924`/);
  assert.match(markdown, /optimization-status evidence rather than an optimized assembly dump/);
  assert.match(markdown, /treebank_e\.xml/);
  assert.match(markdown, /`rawFrameNameId` at\s+`77\.00 MiB\/s`/);
  assert.match(markdown, /`eventObjectFull` counted 75,206,126 public event objects/);
  assert.match(markdown, /`rawFrameNameId` at\s+`76\.08 MiB\/s`/);
  assert.match(markdown, /Bun\/JSC corpus-cycle rows were not\s+bounded under the 512 MiB RSS gate/);
  assert.match(markdown, /Books Corpus-Cycle Countercheck/);
  assert.match(markdown, /Bun\/JSC `attrNameStringOnly` reached `293\.91 MiB\/s`/);
  assert.match(markdown, /Node\/V8\s+`scanAllNoDecode` reached `261\.73 MiB\/s`/);
  assert.match(markdown, /Chrome\/V8 browser\s+`scanAllNoDecode` reached `206\.76 MiB\/s`/);
  assert.match(markdown, /Node\/V8\s+`rawFrameNameId` was the fastest single-run full row at `166\.27 MiB\/s`/);
  assert.match(markdown, /Bun\/JSC `rawFrameNameId` reported `163\.24 MiB\/s`/);
  assert.match(markdown, /`rawFrameSemanticChecksum`/);
  assert.match(markdown, /62,758,976 semantic byte fields with zero fallback string\s+decodes/);
  assert.match(markdown, /bounded repeated-span value caching\s+as `rawFrameStringCache`/);
  assert.match(markdown, /19,818,557 value-cache\s+hits and 67 misses/);
  assert.match(markdown, /same-process\s+`runs=3` stability rerun repeated the selected Node\/V8 and Bun\/JSC full rows/);
  assert.match(markdown, /Node\/V8 `rawFrameNameId` averaged\s+`176\.47 MiB\/s` with `3\.2%` spread/);
  assert.match(markdown, /Bun\/JSC\s+`rawFrameNameId` averaged `178\.52 MiB\/s` with `1\.6%` spread/);
  assert.match(markdown, /long-ascii-text-materialization-candidate-stability\.md/);
  assert.match(markdown, /synchronous\s+`Iterable<Uint8Array\[\]>` byte batches/);
  assert.match(markdown, /Node\/V8 `rawFrameNameId` averaged `172\.85 MiB\/s` with `0\.8%`\s+spread/);
  assert.match(markdown, /`rawFrameNameIdLongAsciiText` averaged only `71\.21 MiB\/s`/);
  assert.match(markdown, /6,370,272 long ASCII text spans with zero fallbacks/);
  assert.match(markdown, /`withoutTextStrings` row averaged `207\.70 MiB\/s` with\s+`26\.3%` spread/);
  assert.match(markdown, /its string-field counter dropped from 62,758,976 to 45,771,584/);
  assert.match(markdown, /text\/CDATA materialization headroom, not a full-StAX counterexample/);
  assert.match(markdown, /text-cache-materialization-candidate-stability\.md/);
  assert.match(markdown, /The control `rawFrameNameId` averaged `175\.02 MiB\/s` with `5\.1%` spread/);
  assert.match(markdown, /`rawFrameNameIdTextCache` row averaged only `129\.31 MiB\/s` with `3\.1%` spread/);
  assert.match(markdown, /16,987,337 text-cache hits and 55 misses/);
  assert.match(markdown, /long-text-cache-materialization-candidate\.md/);
  assert.match(markdown, /`rawFrameNameIdLongTextCache` row improved to `141\.85 MiB\/s` but still stayed\s+below the uncached control/);
  assert.match(markdown, /4,482,765 cache hits and 19 misses/);
  assert.match(markdown, /`withoutTextStrings` row reached\s+`229\.15 MiB\/s`, but changed the checksum to `1372281363`/);
  assert.match(markdown, /negative result for text\/CDATA span caching\s+as a path to the 200 MiB\/s full-StAX target/);
  assert.match(markdown, /fold-trimmed-text-candidate-stability\.md/);
  assert.match(markdown, /avoiding a separate `value\.trim\(\)`\s+result string exposes hidden headroom/);
  assert.match(markdown, /`rawFrameNameId` averaged `122\.32 MiB\/s` with `25\.4%` spread/);
  assert.match(markdown, /`rawFrameNameIdFoldTrim` row averaged only `103\.26 MiB\/s` with `8\.4%` spread/);
  assert.match(markdown, /same 62,758,976 string-field reads,\s+19,818,633 raw span materializations/);
  assert.match(markdown, /avoiding the\s+trim-result allocation is a recorded negative result/);
  assert.match(markdown, /text-cdata-cost-decomposition\.md/);
  assert.match(markdown, /full-parity `rawFrameNameId` control averaged `170\.95 MiB\/s` with `6\.7%` spread/);
  assert.match(markdown, /`rawFrameSemanticChecksum` row folded all 62,758,976\s+semantic byte fields with zero fallback decodes/);
  assert.match(markdown, /still averaged only `152\.09 MiB\/s` with `12\.3%` spread/);
  assert.match(markdown, /`withoutTextStrings` at\s+`219\.85 MiB\/s` with `6\.2%` spread/);
  assert.match(markdown, /string-contract\s+removal signal, not a hidden checksum-folding or trim-allocation optimization/);
  assert.match(markdown, /text-trim-cost-decomposition\.md/);
  assert.match(markdown, /full-parity `rawFrameNameId` control averaged `185\.50 MiB\/s` with `1\.4%` spread/);
  assert.match(markdown, /`rawFrameNameIdNoTrim` averaged\s+`185\.03 MiB\/s` with `1\.4%` spread/);
  assert.match(markdown, /marked\s+non-full-parity because it removes the trimmed-text contract/);
  assert.match(markdown, /`rawFrameNameIdFoldTrim` row averaged\s+`148\.57 MiB\/s`/);
  assert.match(markdown, /`withoutTextStrings` row averaged `249\.13 MiB\/s` with\s+`3\.0%` spread/);
  assert.match(markdown, /14\.50 MiB\/s below 200 MiB\/s/);
  assert.match(markdown, /semantic-checksum-upper-bound\.md/);
  assert.match(markdown, /`rawFrameNameId` reported 96\.88 MiB\/s\s+while `rawFrameSemanticChecksum` reported 94\.11 MiB\/s/);
  assert.match(markdown, /performed zero string-field reads/);
  assert.match(markdown, /direct semantic byte checksum at 0\.97x of its same-condition control/);
  assert.match(markdown, /text-trim-cost-decomposition-2gib\.md/);
  assert.match(markdown, /same trim\/materialization decomposition at 2\.00 GiB/);
  assert.match(markdown, /full-parity `rawFrameNameId` row averaged\s+`184\.92 MiB\/s` with `1\.5%` spread/);
  assert.match(markdown, /114,192,784 events,\s+125,517,686 string-field reads, and checksum `1903859545`/);
  assert.match(markdown, /no-trim row\s+averaged `186\.62 MiB\/s` with `0\.5%` spread/);
  assert.match(markdown, /`withoutTextStrings` row averaged `243\.31 MiB\/s` with max RSS `84\.5 MiB`/);
  assert.match(markdown, />200 MiB\/s signal still appears only after dropping text\/CDATA string\s+materialization/);
  assert.match(markdown, /text-trim-cost-decomposition-4gib\.md/);
  assert.match(markdown, /same corpus-cycle decomposition to 4\.00 GiB/);
  assert.match(markdown, /full-parity `rawFrameNameId` row averaged `178\.86 MiB\/s` with `8\.7%` spread/);
  assert.match(markdown, /228,385,566 events, 251,035,372 string-field reads, and\s+checksum `-1067702969`/);
  assert.match(markdown, /no-trim row averaged `182\.11 MiB\/s` with `7\.4%`\s+spread/);
  assert.match(markdown, /`withoutTextStrings` row\s+averaged `252\.36 MiB\/s` with `3\.7%` spread/);
  assert.match(markdown, /4 GiB full-string row bounded and below the\s+counterexample threshold/);
  assert.match(markdown, /text-trim-cost-decomposition-8gib\.md/);
  assert.match(markdown, /same corpus-cycle decomposition to 8\.00 GiB/);
  assert.match(markdown, /full-parity `rawFrameNameId` row averaged `184\.03 MiB\/s` with `1\.4%` spread/);
  assert.match(markdown, /456,770,888 events, 502,070,478 string-field reads, and\s+checksum `734413569`/);
  assert.match(markdown, /no-trim row averaged `186\.97 MiB\/s` with `0\.6%`\s+spread/);
  assert.match(markdown, /`withoutTextStrings` row\s+averaged `237\.38 MiB\/s` with `4\.5%` spread/);
  assert.match(markdown, /same contract separation observed\s+at 1, 2, and 4 GiB/);
  assert.match(markdown, /access-shape-candidate-stability\.md/);
  assert.match(markdown, /`cursorAccessor` row averaged `138\.00 MiB\/s` with `20\.1%` spread/);
  assert.match(markdown, /`rawFrameDirect`, which decodes every string span directly without\s+name-id caching, averaged `159\.16 MiB\/s`/);
  assert.match(markdown, /control `rawFrameNameId` averaged `175\.40 MiB\/s` with `6\.0%`\s+spread/);
  assert.match(markdown, /cursor and direct raw-frame access shapes as slower than the current name-id\s+raw-frame path/);
  assert.match(markdown, /access-shape-rerun-cross-process-books-corpus\.md/);
  assert.match(markdown, /raw-frame-nameid-alone-cross-process-books-corpus\.md/);
  assert.match(markdown, /did not reproduce the older `177\.34 MiB\/s` Bun\/JSC frontier row/);
  assert.match(markdown, /Node\/V8 reported\s+`cursorAccessor` at `107\.04 MiB\/s`, `rawFrameDirect` at `76\.43 MiB\/s`, and\s+`rawFrameNameId` at `85\.55 MiB\/s`/);
  assert.match(markdown, /Bun\/JSC reported `cursorAccessor` at\s+`114\.05 MiB\/s`, `rawFrameDirect` at `75\.84 MiB\/s`, and `rawFrameNameId` at\s+`93\.33 MiB\/s`/);
  assert.match(markdown, /reported Node\/V8 at `113\.72 MiB\/s` and Bun\/JSC at `118\.58 MiB\/s`/);
  assert.match(markdown, /The old\s+artifact is therefore excluded from current scan\/audit inputs while retained as\s+superseded evidence/);
  assert.match(markdown, /warmup-full-cross-process-books-corpus\.md/);
  assert.match(markdown, /fresh runtime\s+processes with `processRuns=3`, child `runs=1`, and `warmups=1`/);
  assert.match(markdown, /`rawFrameNameIdNoCounters` averaged `93\.96 MiB\/s` with `2\.9%` spread/);
  assert.match(markdown, /regenerated report\s+separates full-string rows from partial rows/);
  assert.match(markdown, /Node\/V8 `stringFull` averaged\s+`113\.79 MiB\/s`[\s\S]*`rawFrameNameId` averaged `94\.98 MiB\/s` with\s+`1\.2%` spread/);
  assert.match(markdown, /Node\/V8 `rawFrameSemanticChecksum` is now recorded as a partial row and\s+averaged `98\.26 MiB\/s`/);
  assert.match(markdown, /Bun\/JSC `stringFull` averaged `120\.18 MiB\/s`[\s\S]*`rawFrameNameId` averaged `97\.84 MiB\/s` with `4\.3%` spread/);
  assert.match(markdown, /Bun\/JSC\s+`rawFrameSemanticChecksum` is now recorded as a partial row and averaged\s+`45\.42 MiB\/s`/);
  assert.match(markdown, /deno-candidate-headroom-cross-process-books-corpus\.md/);
  assert.match(markdown, /Deno\/V8 `stringFull`\s+averaged `81\.48 MiB\/s` with `3\.8%` spread/);
  assert.match(markdown, /Deno\/V8[\s\S]*`rawFrameNameId` averaged `80\.96 MiB\/s`\s+with `7\.3%` spread/);
  assert.match(markdown, /deno-candidate-headroom-cross-process-midsize-corpus\.md/);
  assert.match(markdown, /Deno\/V8 `stringFull` averaged `85\.42 MiB\/s`\s+with `5\.9%` spread/);
  assert.match(markdown, /public `eventObjectFull` averaged `53\.77 MiB\/s` with\s+`4\.3%` spread, and `rawFrameNameId` averaged `72\.37 MiB\/s` with `1\.2%` spread/);
  assert.match(markdown, /preserve 57,096,514 events and checksum `-540013997`, use the same synchronous\s+`Iterable<Uint8Array\[\]>` byte-batch source contract/);
  assert.match(markdown, /candidate-headroom-cross-process-books-corpus-partial\.md/);
  assert.match(markdown, /Only\s+`scanAllNoDecode` stayed above 200 MiB\/s on average/);
  assert.match(markdown, /Node\/V8 averaged\s+`245\.06 MiB\/s`, and Bun\/JSC averaged `314\.50 MiB\/s`/);
  assert.match(markdown, /Large Asset Corpus-Cycle Countercheck/);
  assert.match(markdown, /Node\/V8\s+`rawFrameNameId` averaged `146\.11 MiB\/s` with `15\.5%` spread/);
  assert.match(markdown, /Bun\/JSC was slower and not bounded\s+under the 512 MiB RSS gate/);
  assert.match(markdown, /Chrome\/V8\s+fresh-browser-process books rerun averaged `121\.27 MiB\/s` for `stringFull`/);
  assert.match(markdown, /`104\.90 MiB\/s` for public `eventObjectFull`/);
  assert.match(markdown, /`129\.02 MiB\/s` for\s+`rawFrameNameId`/);
  assert.match(markdown, /browser-fetch-readable-stream-books-corpus\.md/);
  assert.match(markdown, /`fetchReadableStreamFull` row consumes `fetch\(\.\.\.\)\.body` directly through public\s+asynchronous `EventReader`/);
  assert.match(markdown, /`fetchAsyncByteBatchFull` manually groups\s+`Response\.body` reads into `AsyncIterable<Uint8Array\[\]>` batches/);
  assert.match(markdown, /The live rows report only `9\.68 MiB\/s` and `9\.77 MiB\/s`\s+with max used JS heap `34\.1 MiB` and `17\.7 MiB`/);
  assert.match(markdown, /prepared\s+`eventObjectFull` row in the same artifact reports `64\.56 MiB\/s`/);
  assert.match(markdown, /negative result for the hypothesis that direct browser fetch streaming exposes\s+hidden 200 MiB\/s full-StAX headroom/);
  assert.match(markdown, /firefox-fetch-readable-stream-timeout-audit\.md/);
  assert.match(markdown, /did not\s+complete within a 300\.0 second timeout/);
  assert.match(markdown, /implied throughput upper bound\s+of `3\.41 MiB\/s`/);
  assert.match(markdown, /reject this Firefox live\s+fetch path as a plausible 200 MiB\/s full-StAX counterexample/);
  assert.match(markdown, /Firefox\s+fresh-browser-process books rerun averaged `62\.55 MiB\/s` for `stringFull`/);
  assert.match(markdown, /`49\.85 MiB\/s` for public `eventObjectFull`/);
  assert.match(markdown, /`63\.53 MiB\/s` for\s+`rawFrameNameId`/);
  assert.match(markdown, /timing spread was high: `25\.8%`, `28\.6%`, and `33\.7%`/);
  assert.match(markdown, /Chrome\/V8 browser `rawFrameNameId` reported `122\.08 MiB\/s`/);
  assert.match(markdown, /Firefox\/SpiderMonkey `rawFrameNameId` reported\s+`76\.70 MiB\/s`/);
  assert.match(markdown, /57,096,514 events and checksum `-540013997`/);
  assert.match(markdown, /Headless Chrome 148\.0\.0\.0 \/ browser V8/);
  assert.match(markdown, /fastest partial row was\s+`scanAllNoDecode` at `96\.64 MiB\/s`/);
  assert.match(markdown, /fastest full-string parity row was\s+`rawFrameNameId` at `43\.45 MiB\/s`/);
  assert.match(markdown, /`eventObjectFull` reported `33\.10 MiB\/s`/);
  assert.match(markdown, /45,189,256 events, checksum\s+`1421012805`/);
  assert.match(markdown, /`eventObjectFull` counted 45,189,256 public event objects/);
  assert.match(markdown, /Browser variant memory is JS heap, while host process-tree memory is Windows-specific/);
  assert.match(markdown, /max used JS heap `17\.9 MiB`/);
  assert.match(markdown, /`eventObjectFull`\s+recorded max used JS heap `38\.8 MiB`/);
  assert.match(markdown, /max working set `522\.1 MiB`/);
  assert.match(markdown, /max private bytes `283\.9 MiB`/);
  assert.match(markdown, /Firefox\/SpiderMonkey generated rows are recorded separately/);
  assert.match(markdown, /Firefox 143\.0 \/ SpiderMonkey/);
  assert.match(markdown, /built-in WebDriver BiDi endpoint/);
  assert.match(markdown, /does not use Playwright, Selenium, CDP, Node\s+`Buffer\.toString\(\)`, native addons, or lazy getters/);
  assert.match(markdown, /`33\.02 MiB\/s` for `stringFull`/);
  assert.match(markdown, /`23\.31 MiB\/s` for public\s+`eventObjectFull`/);
  assert.match(markdown, /`35\.02 MiB\/s` for `rawFrameNameId`/);
  assert.match(markdown, /All rows preserved\s+45,189,256 events, checksum `1421012805`, and 102,702,850 string-field reads/);
  assert.match(markdown, /`eventObjectFull` counted 45,189,256 public event objects/);
  assert.match(markdown, /Firefox does not expose Chromium `performance\.memory`/);
  assert.match(markdown, /max working set\s+`908\.2 MiB`/);
  assert.match(markdown, /max private bytes `740\.7 MiB`/);
  assert.match(markdown, /`eventObjectFull`\s+`956\.2 MiB` \/ `794\.4 MiB`/);
  assert.match(markdown, /not as portable browser RSS or\s+bounded JS heap proof/);
  assert.match(markdown, /SpiderMonkey\s+codegen\/allocation evidence, Safari\/JSC/);
  assert.match(markdown, /the 85\.4 MiB `treebank_e\.xml` corpus seed repeated as `Uint8Array` byte\s+batches/);
  assert.match(markdown, /fastest partial row was `scanAllNoDecode` at\s+`95\.80 MiB\/s`/);
  assert.match(markdown, /fastest full-string parity\s+row was `stringFull` at `30\.38 MiB\/s`/);
  assert.match(markdown, /`eventObjectFull` reported `28\.92 MiB\/s`/);
  assert.match(markdown, /All full\s+rows preserved 75,206,126 events and checksum\s+`-925527041`/);
  assert.match(markdown, /`eventObjectFull` counted\s+75,206,126 public event objects/);
  assert.match(markdown, /rawFrameNameId` reduced raw span\s+materializations from 75,206,148 to 16,702,403/);
  assert.match(markdown, /58,503,745\s+name-cache hits and 251 misses/);
  assert.match(markdown, /max used JS heap `349\.8 MiB` for `stringFull`/);
  assert.match(markdown, /`358\.4 MiB` for\s+`eventObjectFull`/);
  assert.match(markdown, /max working set `865\.8 MiB`/);
  assert.match(markdown, /max private bytes\s+`689\.2 MiB`/);
  assert.match(markdown, /not a\s+proof of ArrayBuffer external-memory accounting/);
  assert.match(markdown, /Chrome\/V8 browser allocation-sampling artifact recorded same-contract 16 MiB diverse-cycle rows/);
  assert.match(markdown, /Bun\/JSC Memory Allocation Profile/);
  assert.match(markdown, /`rawFrameNameId` at `110\.86 MiB\/s`/);
  assert.match(markdown, /max heap-used endpoints were `12\.9 MiB`,\s+`21\.2 MiB`, and `21\.5 MiB`/);
  assert.match(markdown, /67\.61 MiB\/s for `stringFull`/);
  assert.match(markdown, /51\.44 MiB\/s for public `eventObjectFull`/);
  assert.match(markdown, /65\.85 MiB\/s for `rawFrameNameId`/);
  assert.match(markdown, /sampled allocation totals of 119\.8 KiB, 41\.7 KiB, and 73\.0 KiB/);
  assert.match(markdown, /CDP Chrome\s+148\.0\.7778\.179 \/ browser V8 14\.8\.178\.22/);
  assert.match(markdown, /The sampled rows preserved 706,158 events and checksum `1039217453`/);
  assert.match(markdown, /public\s+event-object row counted 706,158 public event objects/);
  assert.match(markdown, /Top sampled functions included\s+`stax\/index\.js` parser\/materialization frames/);
  assert.match(markdown, /does not cover 1 GiB browser allocation sampling/);
  assert.match(markdown, /browser codegen traces/);
  assert.match(markdown, /non-V8 browser allocation stack\/type traces/);
  assert.match(markdown, /event-reader-string-large\.md/);
  assert.match(markdown, /v8-string-limit-audit\.md/);
  assert.match(markdown, /browser-string-limit-audit\.md/);
  assert.match(markdown, /chrome-v8-source-pin-audit\.md/);
  assert.match(markdown, /chrome-blink-textdecoder-source-pin-audit\.md/);
  assert.match(markdown, /firefox-spidermonkey-string-source-pin-audit\.md/);
  assert.match(markdown, /`JSString` class at line 197/);
  assert.match(markdown, /`PodCopy` at line 2159/);
  assert.match(markdown, /firefox-spidermonkey-textdecoder-source-pin-audit\.md/);
  assert.match(markdown, /firefox-spidermonkey-memory-api-source-pin-audit\.md/);
  assert.match(markdown, /performance\.memory: undefined/);
  assert.match(markdown, /performance\.measureUserAgentSpecificMemory: undefined/);
  assert.match(markdown, /This audit explains the missing row-level JS heap proof; it does not replace a\s+SpiderMonkey allocator stack\/type profile/);
  assert.match(markdown, /firefox-spidermonkey-allocation-profile\.md/);
  assert.match(markdown, /Firefox\/SpiderMonkey Allocation Profile/);
  assert.match(markdown, /ALLOCATION_FACT_LIMIT/);
  assert.match(markdown, /14\s+per-variant host process-tree memory rows/);
  assert.match(markdown, /not row-level JS heap proof/);
  assert.match(markdown, /not portable browser RSS/);
  assert.match(markdown, /bun-webkit-textdecoder-source-pin-audit\.md/);
  assert.match(markdown, /bun-textdecoder-dispatch-source-pin-audit\.md/);
  assert.match(markdown, /node-textdecoder-source-pin-audit\.md/);
  assert.match(markdown, /deno-textdecoder-source-pin-audit\.md/);
  assert.match(markdown, /bun-jsc-string-limit-audit\.md/);
  assert.match(markdown, /bun-jsc-source-pin-audit\.md/);
  assert.match(markdown, /bun-event-reader-string-large\.md/);
  assert.match(markdown, /v8-monomorphic-shape-trace\.md/);
  assert.match(markdown, /v8-allocation-sampling\.md/);
  assert.match(markdown, /deno-v8-allocation-sampling-midsize-corpus\.md/);
  assert.match(markdown, /v8-allocation-sampling-diverse\.md/);
  assert.match(markdown, /v8-allocation-sampling-projection\.md/);
  assert.match(markdown, /event-reader-object/);
  assert.match(markdown, /less-repetitive generated/);
  assert.match(markdown, /two-sample evidence/);
  assert.match(markdown, /generated 16 MiB\s+`projection-cycle` fixture/);
  assert.match(markdown, /stream-event rows preserved 932,199 events and checksum\s+`765158370`/);
  assert.match(markdown, /low selectivity produced 506 records and checksum\s+`-1924876833`/);
  assert.match(markdown, /high selectivity produced 49,063 records and checksum\s+`970928115`/);
  assert.match(markdown, /Projection low\/high\s+sampled `125\.7 KiB` and `104\.3 KiB`/);
  assert.match(markdown, /sampled `stax-xml` source bytes were\s+`88\.8 KiB`, `21\.8 KiB`, `45\.1 KiB`, `29\.6 KiB`, `93\.9 KiB`, and `69\.9 KiB`/);
  assert.match(markdown, /not Bun\/JSC\/browser coverage/);
  assert.match(markdown, /stream-reader-1gb-shapes\.md/);
  assert.match(markdown, /stream-reader-1gb-diverse-shapes\.md/);
  assert.match(markdown, /stream-reader-1gb-allocation-shapes\.md/);
  assert.match(markdown, /stream-reader-1gb-allocation-diverse-shapes\.md/);
  assert.match(markdown, /textdecoder-span-variants\.md/);
  assert.match(markdown, /bun-textdecoder-span-variants\.md/);
  assert.match(markdown, /deno-textdecoder-span-variants\.md/);
  assert.match(markdown, /browser-textdecoder-span-variants\.md/);
  assert.match(markdown, /textdecoder-span-variants-corpus\.md/);
  assert.match(markdown, /bun-textdecoder-span-variants-corpus\.md/);
  assert.match(markdown, /deno-textdecoder-span-variants-corpus\.md/);
  assert.match(markdown, /browser-textdecoder-span-variants-corpus\.md/);
  assert.match(markdown, /firefox-bidi-textdecoder-span-variants\.md/);
  assert.match(markdown, /firefox-bidi-textdecoder-span-variants-corpus\.md/);
  assert.match(markdown, /firefox-bidi-textdecoder-span-cross-process\.md/);
  assert.match(markdown, /firefox-bidi-textdecoder-span-cross-process-corpus\.md/);
  assert.match(markdown, /subarraySharedDecoder` reported `40\.31 MiB\/s`/);
  assert.match(markdown, /Deno\/V8 artifact preserved the same event count and checksum/);
  assert.match(markdown, /Pure\s+`TextDecoder\.decode` rows reported 50\.65 MiB\/s for `subarraySharedDecoder`,\s+50\.66 MiB\/s for `viewSharedDecoder`/);
  assert.match(markdown, /shortAsciiSubarraySharedDecoder` row reported 87\.97 MiB\/s/);
  assert.match(markdown, /Deno\/V8 corpus artifact preserved the same event count and checksum/);
  assert.match(markdown, /Pure\s+`TextDecoder\.decode` rows reported 66\.28 MiB\/s for `subarraySharedDecoder`,\s+63\.44 MiB\/s for `viewSharedDecoder`/);
  assert.match(markdown, /shortAsciiSubarraySharedDecoder` row reported 90\.83 MiB\/s/);
  assert.match(markdown, /Deno 2\.7\.13, V8 14\.7\.173\.20-rusty, and the\s+official `denoland\/deno` source tag `v2\.7\.13`/);
  assert.match(markdown, /`TextDecoder` at line 50, the constructor at line\s+67, the UTF-8 label fast path at line 75/);
  assert.match(markdown, /`op_encoding_decode_utf8\(input, this\.\#ignoreBOM\)` at line 185/);
  assert.match(markdown, /takes `\#\[anybuffer\] zero_copy: &\[u8\]` at line 496/);
  assert.match(markdown, /`v8::String::new_from_utf8` at line\s+520/);
  assert.match(markdown, /does not inspect\s+optimized V8 code, does not count allocations, does not cover browser engines/);
  assert.match(markdown, /midsize\.xml` corpus-seed allocation-sampling follow-up/);
  assert.match(markdown, /Both rows\s+preserved 1,013,762 events and checksum `1553514899`/);
  assert.match(markdown, /`event-reader-object`\s+sampled `98\.9 KiB` across 10 samples/);
  assert.match(markdown, /`raw-frame-name-id-cache` sampled `124\.4 KiB` across 13 samples/);
  assert.match(markdown, /HeadlessChrome 148\.0\.0\.0 \/ browser V8/);
  assert.match(markdown, /Pure `TextDecoder\.decode` rows reported 16\.63 MiB\/s for\s+`subarraySharedDecoder`/);
  assert.match(markdown, /`shortAsciiSubarraySharedDecoder` row reported 39\.42 MiB\/s/);
  assert.match(markdown, /max working set 480\.0 MiB and max private\s+bytes 240\.4 MiB/);
  assert.match(markdown, /Node\/V8 corpus artifact preserved 75,206,126 events and checksum\s+`-925527041`/);
  assert.match(markdown, /`shortAsciiSubarraySharedDecoder` row reported 69\.16 MiB\/s/);
  assert.match(markdown, /every Bun\/JSC\s+corpus row was unbounded under the 512 MiB RSS gate/);
  assert.match(markdown, /1\.41 GiB to 3\.05 GiB/);
  assert.match(markdown, /Chrome\/V8 browser corpus artifact preserved the same event count and\s+checksum/);
  assert.match(markdown, /Pure `TextDecoder\.decode` rows reported 19\.30 MiB\/s for\s+`subarraySharedDecoder`/);
  assert.match(markdown, /`shortAsciiSubarraySharedDecoder` row reported 30\.57 MiB\/s/);
  assert.match(markdown, /All browser\s+corpus rows remained bounded under the artifact's 512 MiB JS heap gate/);
  assert.match(markdown, /max used JS heap from 327\.2 MiB to 332\.7 MiB/);
  assert.match(markdown, /host process-tree max working set 805\.0 MiB and max private bytes 635\.0 MiB/);
  assert.doesNotMatch(markdown, /no browser corpus row for this specific TextDecoder\s+matrix/);
  assert.match(markdown, /shortAsciiSubarraySharedDecoder` row reported 51\.60 MiB\/s/);
  assert.match(markdown, /12,215,015 `TextDecoder` calls and 90,487,835 short-ASCII hits/);
  assert.match(markdown, /TextDecoder span variants \(`subarraySharedDecoder`, `viewSharedDecoder`,/);
  assert.match(markdown, /without V8\s+HeapProfiler allocation sampling overhead/);
  assert.match(markdown, /installed Firefox 143\.0\.1 build's\s+`https:\/\/hg\.mozilla\.org\/releases\/mozilla-release` source stamp\s+`644b498d517849c3fb95679e2017e965fe62b77a`/);
  assert.match(markdown, /`TextDecoder::Init` at line 19/);
  assert.match(markdown, /`Encoding::ForLabelNoReplacement` at line 24/);
  assert.match(markdown, /`NewDecoderWithoutBOMHandling` at line 43/);
  assert.match(markdown, /`NewDecoderWithBOMRemoval` at line 45/);
  assert.match(markdown, /`TextDecoderCommon::DecodeNative` at line 49/);
  assert.match(markdown, /`MaxUTF16BufferLength` at line\s+56/);
  assert.match(markdown, /`GetMutableData` at line 62/);
  assert.match(markdown, /`DecodeToUTF16WithoutReplacement` at line\s+73/);
  assert.match(markdown, /`DecodeToUTF16` at line 80/);
  assert.match(markdown, /`SetLength` at line 86/);
  assert.match(markdown, /`TextDecoder::Decode` at line 102/);
  assert.match(markdown, /`ProcessTypedArrays` at line 110/);
  assert.match(markdown, /`Decoder` storage at line 53/);
  assert.match(markdown, /`Encoding\.h` is adapted from\s+`encoding_rs`/);
  assert.match(markdown, /`mozilla_encoding_decode_to_nsstring` at line 41/);
  assert.match(markdown, /`class Encoding final` at line 154/);
  assert.match(markdown, /`class Decoder final` at line 883/);
  assert.match(markdown, /`decoder_decode_to_utf16` call spanning lines 1008-1010/);
  assert.match(markdown, /`Latin1ByteCompatibleUpTo` at line 1047/);
  assert.match(markdown, /tied to\s+the installed Firefox build used for the browser row/);
  assert.match(markdown, /SpiderMonkey codegen\/allocation evidence/);
  assert.match(markdown, /`warmups=0`, and `runs=3`/);
  assert.match(markdown, /generated byte-batch shape with\s+V8 HeapProfiler allocation sampling/);
  assert.match(markdown, /4,096-row deterministic\s+`diverse-cycle` fixture/);
  assert.match(markdown, /not an allocation profile/);
  assert.match(markdown, /not prove a 200 MiB\/s JavaScript runtime ceiling/);
  assert.match(markdown, /not a proof that no further JavaScript headroom exists/);
  assert.match(markdown, /counterexample-search matrix/);
  assert.match(markdown, /partial rows intentionally keep event-count parity/);
  assert.match(markdown, /128\.8 MiB\/s for `rawFrameNameId`/);
  assert.match(markdown, /No 200 MiB\/s counterexample was found in this\s+matrix/);
  assert.match(markdown, /fastest full-string parity `rawFrameNameId` was 55\.85 MiB\/s/);
  assert.match(markdown, /144\.5 MiB for\s+`rawFrameNameId`/);
  assert.match(markdown, /generated 1\.00 GiB\s+`diverse-cycle` byte-batch fixture/);
  assert.match(markdown, /matrix now includes `eventObjectFull`/);
  assert.match(markdown, /45,189,256 public\s+event objects/);
  assert.match(markdown, /below the artifact's\s+512 MiB bounded-RSS reporting gate/);
  assert.match(markdown, /No 200 MiB\/s bounded-memory counterexample was found in this Node\/V8 1 GiB\s+matrix/);
  assert.match(markdown, /event-reader-byte-batch\.md/);
  assert.match(markdown, /bun-event-reader-byte-batch\.md/);
  assert.match(markdown, /deno-event-reader-byte-batch\.md/);
  assert.match(markdown, /event-reader-byte-batch-corpus\.md/);
  assert.match(markdown, /bun-event-reader-byte-batch-corpus\.md/);
  assert.match(markdown, /deno-event-reader-byte-batch-corpus\.md/);
  assert.match(markdown, /AsyncIterable<Uint8Array\[\]>/);
  assert.match(markdown, /46,195,346 events and checksum\s+`-577171566`/);
  assert.match(markdown, /On Node\/V8, the ReadableStream rows reported 29\.55 MiB\/s at batch size 1,\s+35\.35 MiB\/s at/);
  assert.match(markdown, /byte-batch rows reported 39\.92 MiB\/s at batch size 16 and 40\.85 MiB\/s at batch\s+size 64/);
  assert.match(markdown, /On Bun\/JSC, the ReadableStream rows reported 27\.35 MiB\/s, 29\.90\s+MiB\/s, and 35\.91 MiB\/s/);
  assert.match(markdown, /async byte-batch\s+rows reported 38\.31 MiB\/s at batch size 16 and 38\.88 MiB\/s at batch size 64/);
  assert.match(markdown, /On Deno\/V8, the ReadableStream rows reported 20\.17 MiB\/s, 28\.39 MiB\/s, and\s+29\.38 MiB\/s/);
  assert.match(markdown, /async byte-batch rows reported\s+30\.59 MiB\/s at batch size 16 and 30\.27 MiB\/s at batch size 64/);
  assert.match(markdown, /validates cross-runtime async-boundary headroom and preserves backpressure/);
  assert.match(markdown, /repeat the same source-boundary comparison on the 1\.00 GiB `books\.xml`\s+corpus-cycle fixture/);
  assert.match(markdown, /corpus source is split into 64 KiB `Uint8Array`\s+chunks and repeats only complete corpus cycles/i);
  assert.match(markdown, /not an\s+OS, network, or browser fetch streaming proof/);
  assert.match(markdown, /All corpus rows preserved\s+57,096,514 events and checksum `45154785`/);
  assert.match(markdown, /On Node\/V8, `readableStreamBatch16` reported `45\.79 MiB\/s`,\s+`asyncByteBatch16` reported `45\.68 MiB\/s`, and `syncIterableBatch16` reported\s+`72\.46 MiB\/s`/);
  assert.match(markdown, /On Bun\/JSC, the same rows reported\s+`36\.01 MiB\/s`, `35\.83 MiB\/s`, and `53\.37 MiB\/s`/);
  assert.match(markdown, /On\s+Deno\/V8, they reported `42\.63 MiB\/s`, `42\.04 MiB\/s`, and `67\.61 MiB\/s`/);
  assert.match(markdown, /best current Node\/V8 1 GiB corpus-cycle sync-iterable row is\s+`syncIterableBatch64` at `76\.13 MiB\/s`/);
  assert.match(markdown, /Commit `4282219` tightens the report vocabulary for this exact hypothesis/);
  assert.match(markdown, /`asyncByteBatch\*` is now described as a row that avoids direct\s+`ReadableStream` consumption but still crosses an `AsyncIterator` source\s+boundary/);
  assert.match(markdown, /`syncIterableBatch\*` and `syncFileIterableBatch\*` are the rows that\s+remove both the `ReadableStream` and `AsyncIterator` source boundary/);
  assert.match(markdown, /adds a top-level `parity`\s+object to `event-reader-byte-batch` reports/);
  assert.match(markdown, /headroom evidence against blaming all remaining cost on the JS parser core/);
  assert.match(markdown, /stream-source-consumption-shapes\.md/);
  assert.match(markdown, /machine-readable `sourceContract` records that\s+`sync-iterable-byte-batches` uses `StreamReaderSync` over a synchronous\s+`Iterable<Uint8Array\[\]>`/);
  assert.match(markdown, /primary\s+large comparison rows call `external-baseline` with\s+`--stax-stream-source file-sync-batches`, which records synchronous\s+`Iterable<Uint8Array\[\]>` parser input and `directReadableStream=false`/);
  assert.match(markdown, /`web-readable-stream-pull` uses `StreamReader` over a\s+Web `ReadableStream<Uint8Array>` pull source/);
  assert.match(markdown, /direct ReadableStream row includes the public\s+`StreamReader` `await reader\.read\(\)` boundary/);
  assert.match(markdown, /64 KiB file-backed basis with sync batch sizes 1, 8, and 16,\s+async byte-batch sizes 1, 4, 8, and 16, direct ReadableStream batch sizes 1, 4,\s+8, and 16, plus raw-frame and short-ASCII raw-frame access variants/);
  assert.match(markdown, /fastest sync grouped row at `76\.22 MiB\/s`, the fastest async\s+`Iterable<Uint8Array\[\]>` row as `async-iterable-raw-frame-ascii-batch-8` at\s+`77\.15 MiB\/s`, and the fastest direct ReadableStream row as\s+`web-readable-stream-raw-frame-ascii-batch-8` at `77\.86 MiB\/s`/);
  assert.match(markdown, /short-ASCII raw-frame rows recovered only small source-shape headroom/);
  assert.match(markdown, /run-specific benchmark\s+fact rather than a global\s+async-overhead conclusion/);
  assert.match(markdown, /Its `sourceFacts` section now\s+also pins the implementation evidence/);
  assert.match(markdown, /All rows are parser-demand-driven; async and Web\s+ReadableStream rows carry backpressure metadata/);
  assert.match(markdown, /event-reader-byte-batch-cross-process-corpus\.md/);
  assert.match(markdown, /event-reader-byte-batch-cross-process-midsize-corpus\.md/);
  assert.match(markdown, /Node\/V8 averaged\s+`32\.46 MiB\/s` for `readableStreamBatch16`, `30\.16 MiB\/s` for\s+`asyncByteBatch16`, `59\.97 MiB\/s` for `syncIterableBatch16`, and\s+`56\.76 MiB\/s` for `syncFileIterableBatch16`/);
  assert.match(markdown, /file-backed sync\s+`Iterable<Uint8Array\[\]>` rows in three fresh processes per runtime on the same\s+1\.00 GiB `books\.xml` corpus-cycle fixture/);
  assert.match(markdown, /read corpus chunks from the OS file source with `readSync`/);
  assert.match(markdown, /not browser\s+fetch streaming proof/);
  assert.match(markdown, /regenerated report now carries row-level source\s+metadata/);
  assert.match(markdown, /`readableStreamBatch16` is `ReadableStream<Uint8Array>` with\s+`directReadableStream=true`, `asyncBoundary=true`, `respectsBackpressure=true`,\s+and `fullArrayBufferParserInput=false`/);
  assert.match(markdown, /`asyncByteBatch16` is\s+`AsyncIterable<Uint8Array\[\]>` with `directReadableStream=false` but\s+`asyncBoundary=true`/);
  assert.match(markdown, /both sync rows are `Iterable<Uint8Array\[\]>` with\s+`asyncBoundary=false`/);
  assert.match(markdown, /Node\/V8 reported `52\.76 MiB\/s` for direct `ReadableStream`,\s+`47\.82 MiB\/s` for async byte batches, `75\.62 MiB\/s` for prepared sync batches,\s+and `67\.30 MiB\/s` for file-backed sync batches/);
  assert.match(markdown, /Bun\/JSC reported `38\.74`,\s+`35\.65`, `51\.13`, and `47\.98 MiB\/s`; Deno\/V8 reported `45\.92`, `41\.51`,\s+`64\.72`, and `59\.59 MiB\/s`/);
  assert.match(markdown, /Prepared sync batches were `1\.43x`, `1\.32x`, and\s+`1\.41x` the direct ReadableStream rows respectively, while file-backed sync\s+batches were `1\.28x`, `1\.24x`, and `1\.30x`/);
  assert.match(markdown, /file-backed-core-decomposition\.md` now runs\s+the same 1\.00 GiB file-backed source shape used by the fastest source sweep\s+candidate \(`chunkKiB=32`, `batchSize=4`\)/);
  assert.match(markdown, /`stax-scan-all-no-decode` reaches 216\.08 MiB\/s and\s+`stax-raw-frame-span-stats` reaches 210\.19 MiB\/s with bounded RSS, but both drop\s+the full-string checksum contract/);
  assert.match(markdown, /public `stax-stream` at\s+125\.99 MiB\/s, and full-string `stax-raw-frame-name-id` at 129\.66 MiB\/s with the\s+shared event count `61,236,571` and full checksum `-716099804`/);
  assert.match(markdown, /file-backed-fold-trim-candidate\.md` records\s+`stax-raw-frame-name-id` at 138\.13 MiB\/s and\s+`stax-raw-frame-name-id-fold-trim` at 113\.11 MiB\/s with the same full checksum/);
  assert.match(markdown, /file-backed-string-cache-candidate\.md` records\s+`stax-raw-frame-name-id` at 137\.51 MiB\/s and `stax-raw-frame-string-cache` at\s+49\.64 MiB\/s with max RSS 616\.98 MiB/);
  assert.match(markdown, /file-backed-short-attr-value-cache-candidate\.md` narrows the cache idea to\s+attribute values with byte length at most four/);
  assert.match(markdown, /`stax-raw-frame-short-attr-value-cache` averaged 140\.15 MiB\/s with max RSS\s+67\.01 MiB/);
  assert.match(markdown, /same-fixture\s+Woodstox at 338\.14 MiB\/s and quick-xml at 274\.63 MiB\/s/);
  assert.match(markdown, /file-backed-trim-boundary-check-candidate\.md` tests whether the checksum\s+consumer can skip `value\.trim\(\)`/);
  assert.match(markdown, /`stax-raw-frame-name-id-trim-boundary-check` averaged 130\.27 MiB\/s with max RSS\s+67\.22 MiB/);
  assert.match(markdown, /same-fixture\s+Woodstox at 351\.56 MiB\/s and quick-xml at 273\.74 MiB\/s/);
  assert.match(markdown, /firefox-spidermonkey-profiler-trace\.md/);
  assert.match(markdown, /Gecko Profiler startup\/shutdown evidence/);
  assert.match(markdown, /7 threads, 9,054 samples, and 9,640 frames/);
  assert.match(markdown, /not JIT IR, not a deterministic\s+optimized-code dump/);
  assert.match(markdown, /firefox-spidermonkey-jitspew-source-pin-audit\.md/);
  assert.match(markdown, /firefox-spidermonkey-buildconfig-source-pin-audit\.md/);
  assert.match(markdown, /about:buildconfig` excerpt records `--enable-js-shell` and\s+`MOZ_PACKAGE_JSSHELL=1`, but no `--enable-jitspew`, `JS_JITSPEW`, or\s+`JS_STRUCTURED_SPEW`/);
  assert.match(markdown, /pins\s+Gecko revision `644b498d517849c3fb95679e2017e965fe62b77a`/);
  assert.match(markdown, /records the\s+`JS_JITSPEW` guard in `JitSpewer\.h` and `JitSpewer\.cpp`/);
  assert.match(markdown, /`--enable-jitspew` configure option/);
  assert.match(markdown, /firefox-spidermonkey-diagnostic-dump-audit\.md/);
  assert.match(markdown, /`IONFLAGS=logs,codegen,mir,lir,aborts,scripts`/);
  assert.match(markdown, /preserving full parity for 4,985 events and\s+checksum `1856142966`/);
  assert.match(markdown, /emitted no dump files and no recognizable diagnostic\s+stream output/);
  assert.match(markdown, /`status=no-dump-emitted`, `dumpFiles=0`/);
  assert.match(markdown, /JitSpew source gate and\s+diagnostic no-dump attempt are present/);
  assert.match(markdown, /with the no-dump outcome preserved/);
  assert.match(markdown, /firefox-spidermonkey-js-shell-availability-audit\.md/);
  assert.match(markdown, /checks the local `SPIDERMONKEY_JS_SHELL`, `JSSHELL`, and `JS_SHELL`/);
  assert.match(markdown, /finds two local candidates/);
  assert.match(markdown, /G:\\tmp\\stax-spidermonkey-jsshell\\extract\\js\.exe/);
  assert.match(markdown, /G:\\tmp\\stax-spidermonkey-nightly-jsshell\\js\.exe/);
  assert.match(markdown, /moves the local shell availability artifact from a missing-shell\s+`NEGATIVE_RESULT` to `ENVIRONMENT_FACT`/);
  assert.match(markdown, /firefox-spidermonkey-release-jsshell-availability-audit\.md/);
  assert.match(markdown, /official Firefox `143\.0\.1` release jsshell package/);
  assert.match(markdown, /verifies `jsshell-win64\.zip` against\s+`SHA512SUMS`/);
  assert.match(markdown, /runs `js\.exe --version` as `JavaScript-C143\.0\.1`/);
  assert.match(markdown, /observes\s+`ionHits=4988`, `ion\.enable=1`, `ion\.warmup\.trigger=0`, and checksum\s+`12502500`/);
  assert.match(markdown, /records `disnative`, `disblic`, and `inJit`\s+builtins, but `hasDisassembler\(\): false`/);
  assert.match(markdown, /creates\s+a 93-byte file and then/);
  assert.match(markdown, /fails with `Error: Did not write all function bytes to\s+the file\.`/);
  assert.match(markdown, /can read the `books\.xml` probe as binary `Uint8Array`\s+\(`4551` bytes, checksum `356012`\)/);
  assert.match(markdown, /lacks `TextDecoder`, `TextEncoder`,\s+`ReadableStream`, and `fetch`, so it cannot run the current full-string\s+stax-xml benchmark unchanged/);
  assert.match(markdown, /records no JitSpew, `IONFLAGS`, MIR\/LIR,\s+or IR dump flag surface/);
  assert.match(markdown, /The same\s+`IONFLAGS`\/`JIT_SPEW` environment probe also reports `no-jitspew-output`, zero\s+diagnostic markers, and zero stderr lines/);
  assert.match(markdown, /does not close the\s+`codegen-traces-open` obligation/);
  assert.match(markdown, /firefox-spidermonkey-nightly-jsshell-availability-audit\.md/);
  assert.match(markdown, /official Mozilla nightly jsshell package from\s+`latest-mozilla-central` as of 2026-05-31/);
  assert.match(markdown, /build id `20260531212007`, and source revision\s+`71e37c8757f87e7682d7db7d9b9ec9f7f81e24f7`/);
  assert.match(markdown, /packageVerified=false/);
  assert.match(markdown, /runs as `JavaScript-C153\.0a1`/);
  assert.match(markdown, /observes the same `ionHits=4988`, `ion\.enable=1`, `ion\.warmup\.trigger=0`, and\s+checksum `12502500`/);
  assert.match(markdown, /has `hasDisassembler\(\): false`, creates only a failed 83-byte `disnative` file/);
  assert.match(markdown, /narrows one more\s+public Mozilla package path/);
  assert.match(markdown, /does not close\s+`codegen-traces-open`/);
  assert.match(markdown, /same-process `runs=3`, `warmups=0` rerun/);
  assert.match(markdown, /`scanAllNoDecode` at `109\.41 MiB\/s`/);
  assert.match(markdown, /timing spread was\s+high at `25\.2%`/);
  assert.match(markdown, /`eventObjectFull` reported `43\.75 MiB\/s` with `2\.7%` spread/);
  assert.match(markdown, /`rawFrameNameId` reported `58\.48 MiB\/s` with `0\.7%` spread/);
  assert.match(markdown, /max RSS was `103\.6 MiB` for `stringFull`, `206\.2 MiB` for\s+`eventObjectFull`, and `220\.9 MiB` for `rawFrameNameId`/);
  assert.match(markdown, /regenerated Bun\/JSC partial rows did not reproduce the previous 200 MiB\/s\+/);
  assert.match(markdown, /`scanAllNoDecode` reached `176\.15 MiB\/s` with\s+max RSS `199\.6 MiB`/);
  assert.match(markdown, /`eventObjectFull` reported `37\.27 MiB\/s`/);
  assert.match(markdown, /current artifact is not a counterexample to a broad 200 MiB\/s Bun\/JSC\s+byte-batch claim/);
  assert.match(markdown, /`rawFrameNameId` reported `57\.99 MiB\/s`/);
  assert.match(markdown, /177\.0 MiB for\s+`eventObjectFull`/);
  assert.match(markdown, /current Bun\/JSC generated rows still have bounded\s+memory, but no row reaches 200 MiB\/s/);
  assert.match(markdown, /### Bun\/JSC 1 GiB Candidate Headroom Stability Rerun/);
  assert.match(markdown, /same-process `runs=3`, `warmups=0` rerun for the same generated 1\.00 GiB\s+`diverse-cycle` byte-batch fixture/);
  assert.match(markdown, /`scanAllNoDecode` at `139\.49 MiB\/s`/);
  assert.match(markdown, /high `36\.8%` timing\s+spread across samples `5562\.83`, `8196\.78`, and `8263\.59` ms/);
  assert.match(markdown, /`stringFull` reported `54\.24 MiB\/s` with `3\.2%` spread/);
  assert.match(markdown, /`eventObjectFull` reported `37\.63 MiB\/s` with `2\.9%` spread/);
  assert.match(markdown, /`rawFrameNameId` reported `58\.73 MiB\/s` with `2\.2%` spread/);
  assert.match(markdown, /max RSS was `185\.5 MiB` for `stringFull`, `180\.7 MiB` for `eventObjectFull`,\s+and `191\.8 MiB` for `rawFrameNameId`/);
  assert.match(markdown, /not a\s+cross-process statistical proof and it does not cover browsers, corpus fixtures,\s+projection rows, or Bun\/JSC machine-code\/allocation traces/);
  assert.match(markdown, /64 MiB `TRACE_FACT`/);
  assert.match(markdown, /collected 329 samples over 2,547\.57 ms/);
  assert.match(markdown, /38\.78% in `stax-xml` bundled frames/);
  assert.match(markdown, /`scanAllNoDecode` at 223\.32 MiB\/s/);
  assert.match(markdown, /`stringFull` at 102\.29 MiB\/s/);
  assert.match(markdown, /`eventObjectFull` at 73\.39 MiB\/s/);
  assert.match(markdown, /`rawFrameNameId` at 108\.77 MiB\/s/);
  assert.match(markdown, /full rows preserved 2,824,406 events and checksum `288962256`/);
  assert.match(markdown, /materializePublicEventObject/);
  assert.match(markdown, /not a JavaScriptCore codegen\/IR\/assembly trace/);
  assert.match(markdown, /partial `scanAllNoDecode` row crosses\s+200 MiB\/s on this 64 MiB profiler run/);
  assert.match(markdown, /drops all strings and is not a\s+full StAX counterexample/);
  assert.match(markdown, /fastest full row, `rawFrameNameId`, remains below\s+200 MiB\/s/);
  assert.match(markdown, /65,536-row `diverse-cycle` fixture/);
  assert.match(markdown, /wide-cycle Bun\/JSC run is a cache-sensitivity check/);
  assert.match(markdown, /still not a full-string 200 MiB\/s counterexample/);
  assert.match(markdown, /older 200 MiB\/s\+ partial rows should not be treated as stable current\s+evidence/);
  assert.match(markdown, /`eventObjectFull` reported `34\.09 MiB\/s`/);
  assert.match(markdown, /1\.00 GiB `projection-cycle` fixture/);
  assert.match(markdown, /Low selectivity selects `\/root\/book\[@code="7"\]`/);
  assert.match(markdown, /high\s+selectivity selects every `\/root\/book`/);
  assert.match(markdown, /Node\/V8\s+reported 60,416,563 stream events and full-row checksum `1441552024`/);
  assert.match(markdown, /fastest\s+full row was `rawFrameNameId` at `82\.91 MiB\/s`/);
  assert.match(markdown, /public `eventObjectFull`\s+was `57\.67 MiB\/s`/);
  assert.match(markdown, /Bun\/JSC preserved the same stream-event count and checksum/);
  assert.match(markdown, /fastest full row was `rawFrameNameId` at `84\.68 MiB\/s`/);
  assert.match(markdown, /public\s+`eventObjectFull` was `63\.29 MiB\/s`/);
  assert.match(markdown, /Node\/V8 `projectionLowSelectivity` reported\s+`127\.79 MiB\/s`, 33,382 records, checksum `-403434369`, 66,764 selected string\s+fields, and max RSS `156\.1 MiB`/);
  assert.match(markdown, /`projectionHighSelectivity` reported\s+`99\.60 MiB\/s`, 3,179,819 records, checksum `-2078190377`, 6,359,638 selected\s+string fields, and max RSS `156\.1 MiB`/);
  assert.match(markdown, /Bun\/JSC `projectionLowSelectivity`\s+reported `126\.08 MiB\/s`/);
  assert.match(markdown, /`projectionHighSelectivity` reported `75\.84 MiB\/s`/);
  assert.match(markdown, /older Bun\/JSC\s+`scanAllNoDecode` single-run row in this same artifact reached `245\.41 MiB\/s`/);
  assert.match(markdown, /it drops all strings/);
  assert.match(markdown, /did not reproduce as a 200 MiB\/s\+ average in the `runs=3` stability artifact/);
  assert.match(markdown, /Node\/V8 reported `scanAllNoDecode` at `151\.85 MiB\/s` with `37\.8%` spread/);
  assert.match(markdown, /`rawFrameNameId` at `82\.47 MiB\/s` with\s+`0\.7%` spread/);
  assert.match(markdown, /Node\/V8 projection rows reported `projectionLowSelectivity` at\s+`128\.02 MiB\/s` with `2\.9%` spread/);
  assert.match(markdown, /`projectionHighSelectivity` at\s+`99\.34 MiB\/s` with `2\.3%` spread/);
  assert.match(markdown, /Bun\/JSC reported `scanAllNoDecode` at `173\.22 MiB\/s` with `44\.8%` spread/);
  assert.match(markdown, /`rawFrameNameId` at `83\.10 MiB\/s` with\s+`0\.5%` spread/);
  assert.match(markdown, /Bun\/JSC `projectionLowSelectivity` reported `125\.90 MiB\/s` with\s+`1\.9%` spread/);
  assert.match(markdown, /`projectionHighSelectivity` reported\s+`89\.98 MiB\/s` with high `31\.6%` spread/);
  assert.match(markdown, /fresh runtime processes/);
  assert.match(markdown, /`processRuns=3`, child `runs=1`, `warmups=0`/);
  assert.match(markdown, /Node\/V8 averaged `90\.40 MiB\/s` for `stringFull`/);
  assert.match(markdown, /`57\.56 MiB\/s` for public `eventObjectFull`/);
  assert.match(markdown, /`82\.01 MiB\/s` for\s+`rawFrameNameId`/);
  assert.match(markdown, /`128\.98 MiB\/s` for low-selectivity projection/);
  assert.match(markdown, /`99\.42 MiB\/s` for high-selectivity projection/);
  assert.match(markdown, /Bun\/JSC averaged\s+`97\.67 MiB\/s`, `63\.09 MiB\/s`, `83\.82 MiB\/s`, `125\.78 MiB\/s`, and\s+`69\.87 MiB\/s`/);
  assert.match(markdown, /Deno\/V8 averaged `87\.42 MiB\/s`,\s+`56\.53 MiB\/s`, `76\.92 MiB\/s`, `120\.77 MiB\/s`, and `91\.39 MiB\/s`/);
  assert.match(markdown, /Node\/V8 maxed at\s+`160\.2 MiB`, Bun\/JSC maxed at `205\.3 MiB`, and Deno\/V8 maxed at `159\.7 MiB`/);
  assert.match(markdown, /fresh-process Bun\/JSC high-selectivity projection average did not reproduce/);
  assert.match(markdown, /Deno\/V8 also stayed below Node\/V8/);
  assert.match(markdown, /selected-field projection rows are headroom evidence, not full StAX counterexamples/);
  assert.match(markdown, /Browser Chrome\/V8 1 GiB Projection-Cycle Candidate Headroom Matrix/);
  assert.match(markdown, /browser counterpart of the projection-cycle\s+matrix/);
  assert.match(markdown, /fastest partial row was\s+`scanAllNoDecode` at `128\.27 MiB\/s`/);
  assert.match(markdown, /fastest full-string parity row was\s+`rawFrameNameId` at `60\.32 MiB\/s`/);
  assert.match(markdown, /public `eventObjectFull` reported `48\.12 MiB\/s`/);
  assert.match(markdown, /`projectionLowSelectivity` reported `104\.33 MiB\/s`, 33,382 records, checksum\s+`-403434369`, 66,764 selected string fields, and max used JS heap `34\.0 MiB`/);
  assert.match(markdown, /`projectionHighSelectivity` reported `69\.37 MiB\/s`, 3,179,819 records,\s+checksum `-2078190377`, 6,359,638 selected string fields, and max used JS heap\s+`19\.0 MiB`/);
  assert.match(markdown, /max working set `444\.3 MiB` and max private\s+bytes `210\.5 MiB`/);
  assert.match(markdown, /same-process `runs=3`, `warmups=0`/);
  assert.match(markdown, /`rawFrameNameId` at `60\.12 MiB\/s` with\s+`1\.0%` spread/);
  assert.match(markdown, /public `eventObjectFull` at\s+`47\.42 MiB\/s` with `6\.0%` spread/);
  assert.match(markdown, /`projectionLowSelectivity` reported `103\.92 MiB\/s` with `1\.7%` spread/);
  assert.match(markdown, /`projectionHighSelectivity` reported `72\.07 MiB\/s` with `4\.8%` spread/);
  assert.match(markdown, /max used JS heap `55\.6 MiB` for `rawFrameNameId`/);
  assert.match(markdown, /max working set `518\.8 MiB` and max private bytes\s+`280\.1 MiB`/);
  assert.match(markdown, /fresh-browser-process Chrome\/V8 3x projection rerun averaged 63\.63 MiB\/s/);
  assert.match(markdown, /`48\.33 MiB\/s` for public `eventObjectFull`/);
  assert.match(markdown, /`60\.34 MiB\/s` for\s+`rawFrameNameId`/);
  assert.match(markdown, /`104\.64 MiB\/s` for low-selectivity projection/);
  assert.match(markdown, /`70\.78 MiB\/s` for high-selectivity projection/);
  assert.match(markdown, /Max used JS heap endpoints were\s+`11\.5 MiB`, `21\.5 MiB`, `33\.6 MiB`, `32\.5 MiB`, and `14\.6 MiB`/);
  assert.match(markdown, /max working set\s+`449\.0 MiB` and max private bytes `215\.1 MiB`/);
  assert.match(markdown, /104\.64 MiB\/s with 1\.4% spread in the fresh-process rerun/);
  assert.match(markdown, /70\.78 MiB\/s with 2\.4% fresh-process spread/);
  assert.match(markdown, /Chrome\/V8\/Firefox-SpiderMonkey fresh-browser-process projection-cycle/);
  assert.match(markdown, /Firefox\/SpiderMonkey 1 GiB Projection-Cycle Candidate Headroom Matrix/);
  assert.match(markdown, /`stringFull` reported `61\.77 MiB\/s`/);
  assert.match(markdown, /public `eventObjectFull` reported\s+`52\.54 MiB\/s`/);
  assert.match(markdown, /`rawFrameNameId` reported `64\.24 MiB\/s`/);
  assert.match(markdown, /`projectionLowSelectivity` reported\s+`115\.30 MiB\/s`, 33,382 records/);
  assert.match(markdown, /`projectionHighSelectivity` reported `82\.57 MiB\/s`, 3,179,819\s+records/);
  assert.match(markdown, /fresh Firefox processes/);
  assert.match(markdown, /`stringFull` averaged `63\.42 MiB\/s`/);
  assert.match(markdown, /public `eventObjectFull` averaged `53\.75 MiB\/s`/);
  assert.match(markdown, /`rawFrameNameId` averaged `69\.08 MiB\/s`/);
  assert.match(markdown, /`projectionLowSelectivity` averaged `119\.99 MiB\/s`/);
  assert.match(markdown, /`projectionHighSelectivity` averaged `88\.26 MiB\/s`/);
  assert.match(markdown, /Firefox\/SpiderMonkey generated TextDecoder span artifact/);
  assert.match(markdown, /shortAsciiSubarraySharedDecoder` row\s+reported `46\.28 MiB\/s`/);
  assert.match(markdown, /Firefox\/SpiderMonkey corpus TextDecoder span artifact/);
  assert.match(markdown, /shortAsciiSubarraySharedDecoder` row\s+reported `68\.42 MiB\/s`/);
  assert.match(markdown, /Firefox\/SpiderMonkey fresh-process TextDecoder span artifact/);
  assert.match(markdown, /shortAsciiSubarraySharedDecoder` averaged `49\.96 MiB\/s`/);
  assert.match(markdown, /fresh-process corpus TextDecoder span artifact/);
  assert.match(markdown, /shortAsciiSubarraySharedDecoder` averaged `69\.09 MiB\/s`/);
  assert.match(markdown, /The 512 MiB row\s+succeeded at 74\.19 MiB\/s/);
  assert.match(markdown, /peak RSS 1\.07 GiB/);
  assert.match(markdown, /RangeError: Invalid string length/);
  assert.match(markdown, /MAX_STRING_LENGTH` is `536,870,888` UTF-16 code units/);
  assert.match(markdown, /\(1 << 29\) - 24/);
  assert.match(markdown, /Code-unit headroom below `MAX_STRING_LENGTH`\s+was `748,090`/);
  assert.match(markdown, /not a byte-batch runtime ceiling/);
  assert.match(markdown, /CDP Chrome 148\.0\.7778\.179/);
  assert.match(markdown, /V8 14\.8\.178\.22/);
  assert.match(markdown, /64 MiB complete XML string control row/);
  assert.match(markdown, /2,824,406\s+public event objects/);
  assert.match(markdown, /6,419,100 string-field reads/);
  assert.match(markdown, /checksum `288962256`/);
  assert.match(markdown, /throughput `81\.08 MiB\/s`/);
  assert.match(markdown, /max used JS\s+heap `150\.2 MiB`/);
  assert.match(markdown, /max\s+working set `614\.9 MiB` and max private bytes `358\.8 MiB`/);
  assert.match(markdown, /`1,072,245,626` UTF-16 code units/);
  assert.match(markdown, /failed string\s+construction before parsing with `RangeError: Invalid string length`/);
  assert.match(markdown, /official `v8\/v8` source tag `refs\/tags\/14\.8\.178\.22`/);
  assert.match(markdown, /`String::kMaxLength` line 126/);
  assert.match(markdown, /line 127, which resolves to 536,870,888 UTF-16 code units/);
  assert.match(markdown, /exceeding the pinned V8 limit by 535,374,738\s+code units/);
  assert.match(markdown, /`String::NewFromUtf8` at line 485/);
  assert.match(markdown, /`NewExternalOneByte`\s+line 550/);
  assert.match(markdown, /not a\s+throughput proof/);
  assert.match(markdown, /Chrome\/Blink TextDecoder Source Pin Audit/);
  assert.match(markdown, /Chrome 148\.0\.7778\.179, V8\s+14\.8\.178\.22/);
  assert.match(markdown, /Chromium source ref `refs\/tags\/148\.0\.7778\.179`/);
  assert.match(markdown, /`TextDecoder::decode` at line 81/);
  assert.match(markdown, /delegation to `Decode` at line 93/);
  assert.match(markdown, /`NewTextCodec` at line 108/);
  assert.match(markdown, /`codec_->Decode` at line 120/);
  assert.match(markdown, /`TextCodecUtf8::Decode` at line 356/);
  assert.match(markdown, /`InlinedStringBuffer<LChar>` at line\s+365/);
  assert.match(markdown, /ASCII fast path at line 394/);
  assert.match(markdown, /`CopyAsciiMachineWord` at line 402/);
  assert.match(markdown, /LChar\s+`ToString` at line 443/);
  assert.match(markdown, /`InlinedStringBuffer<UChar>` at line 446/);
  assert.match(markdown, /UChar `ToString` at line 529/);
  assert.match(markdown, /does not cover Node, Bun\/JSC, Safari\/JSC, or Firefox\/SpiderMonkey\s+TextDecoder internals/);
  assert.match(markdown, /Bun-Patched WebKit TextDecoder Source Pin Audit/);
  assert.match(markdown, /Bun artifact runtime 1\.3\.13 and WebKit commit\s+`4d5e75ebd84a14edbc7ae264245dcd77fe597c10`/);
  assert.match(markdown, /`TextDecoder::decode` at line 55/);
  assert.match(markdown, /`BufferSource` span extraction at line 61/);
  assert.match(markdown, /`newTextCodec` at line 65/);
  assert.match(markdown, /`TextCodec::decode` call at line 71/);
  assert.match(markdown, /decoded\s+`String` return at line 78/);
  assert.match(markdown, /`TextCodec::decode` virtual\s+method at line 53/);
  assert.match(markdown, /`TextCodecUTF8::registerCodecs` at line 67/);
  assert.match(markdown, /`TextEncodingRegistry` UTF-8 registration at line 211/);
  assert.match(markdown, /`newTextCodec` registry\s+lookup at line 292/);
  assert.match(markdown, /`TextCodecUTF8::decode` at line 304/);
  assert.match(markdown, /`StringBuffer<Latin1Character>` at line 314/);
  assert.match(markdown, /ASCII fast path at line 336/);
  assert.match(markdown, /`copyASCIIMachineWord` at line 342/);
  assert.match(markdown, /Latin1 `String::adopt` at line 393/);
  assert.match(markdown, /`StringBuffer<char16_t>` at line 396/);
  assert.match(markdown, /`appendCharacter` at line 465/);
  assert.match(markdown, /UTF-16 `String::adopt` at line 478/);
  assert.match(markdown, /does not prove that Bun runtime\s+dispatch reaches these exact functions/);
  assert.match(markdown, /Bun TextDecoder Dispatch Source Pin Audit/);
  assert.match(markdown, /source tag `bun-v1\.3\.13`/);
  assert.match(markdown, /source commit `bf2e2cecf27e800962b1e7f03d66278f9d5d2e79`/);
  assert.match(markdown, /local runtime\s+`1\.3\.13\+bf2e2cecf`/);
  assert.match(markdown, /`TextDecoder` at line 5/);
  assert.match(markdown, /prototype `decode` to `fn: "decode"` at line\s+24/);
  assert.match(markdown, /DOMJIT `JSString` return plus `JSUint8Array` argument metadata\s+at lines 28-29/);
  assert.match(markdown, /`JSTextDecoder` codegen at line\s+21/);
  assert.match(markdown, /default UTF-8 encoding at line 19/);
  assert.match(markdown, /`TextDecoder\.decode` at line 158/);
  assert.match(markdown, /`decodeWithoutTypeChecks` at line 188/);
  assert.match(markdown, /`decodeSlice` at line 192/);
  assert.match(markdown, /encoding switch at line 195/);
  assert.match(markdown, /Bun Zig UTF-8 branch at line 211/);
  assert.match(markdown, /`strings\.toUTF16AllocMaybeBuffered` at line 230/);
  assert.match(markdown, /`ZigString\.toExternalU16` at\s+line 253/);
  assert.match(markdown, /ASCII `ZigString\.init\(input\)\.toJS` at line 259/);
  assert.match(markdown, /all-other-encodings branch/);
  assert.match(markdown, /source comment\s+is pinned at line 280/);
  assert.match(markdown, /`TextCodec\.create` at line 286/);
  assert.match(markdown, /`codec\.decode` at\s+line 298/);
  assert.match(markdown, /native UTF-8,\s+UTF-16, and Latin1 support are not registered in the codec map at line 207/);
  assert.match(markdown, /UTF-8 fallback returns null because UTF-8 is handled natively in Bun at\s+line 296/);
  assert.match(markdown, /counterexample to citing the pinned WebKit `WebCore\/dom\/TextDecoder\.cpp`\s+path as the Bun 1\.3\.13 default UTF-8 TextDecoder benchmark path/);
  assert.match(markdown, /Node TextDecoder Source Pin Audit/);
  assert.match(markdown, /Node v24\.15\.0, V8 13\.6\.233\.17-node\.48/);
  assert.match(markdown, /`TextDecoder\.decode` at line 482/);
  assert.match(markdown, /UTF-8 fast path call to `decodeUTF8` at line 494/);
  assert.match(markdown, /`BindingData::DecodeUTF8` line 151/);
  assert.match(markdown, /`ArrayBufferViewContents<char>` line\s+164/);
  assert.match(markdown, /`StringBytes::Encode\(\.\.\., UTF8\)` at line 203/);
  assert.match(markdown, /`StringBytes::Encode` at line\s+503/);
  assert.match(markdown, /UTF-8 branch at line 541/);
  assert.match(markdown, /UTF-8 ASCII\s+`ExternOneByteString::NewFromCopy` path at line 548/);
  assert.match(markdown, /`String::NewFromUtf8` fallback at\s+line 571/);
  assert.match(markdown, /does not make Node\s+`Buffer\.toString\(\)` a neutral browser-compatible lane/);
  assert.match(markdown, /does not cover Blink,\s+WebKit, or SpiderMonkey TextDecoder internals/);
  assert.match(markdown, /browser Chrome\/V8 string-limit audit also failed the projected 1024 MiB complete XML string before parsing/);
  assert.match(markdown, /non-V8 browser feasibility rows/);
  assert.match(markdown, /StringImpl::MaxLength` is `2,147,483,647` UTF-16\s+code units/);
  assert.match(markdown, /std::numeric_limits<int32_t>::max\(\)/);
  assert.match(markdown, /1024 MiB projection is below the Bun\/JSC\s+StringImpl::MaxLength/);
  assert.match(markdown, /JSC\s+code-unit headroom was `1,075,238,021`/);
  assert.match(markdown, /counterexample to porting the\s+V8 complete-string size conclusion directly to Bun\/JSC/);
  assert.match(markdown, /`JSString` as a `JSCell` at\s+line 102/);
  assert.match(markdown, /constructor storing a `WTF::String` at line 168/);
  assert.match(markdown, /`allocateCell<JSString>` construction at line 207/);
  assert.match(markdown, /`WTF::String` as holding `RefPtr<StringImpl>` at line 348/);
  assert.match(markdown, /`StringImpl::createInternal` at line 269/);
  assert.match(markdown, /uninitialized-storage call at line 274/);
  assert.match(markdown, /`copyCharacters` call at\s+line 275/);
  assert.match(markdown, /`StringImpl::create8BitIfPossible` line 336/);
  assert.match(markdown, /`copyElements` line 344/);
  assert.match(markdown, /JSC JavaScript string is an engine-owned\s+value backed by `JSString` \/ `WTF::String` \/ `StringImpl` storage/);
  assert.match(markdown, /does\s+not prove that Bun\/JSC has no remaining JS-runtime headroom/);
  assert.match(markdown, /Bun\/JSC parsed the 1\.00 GiB complete string/);
  assert.match(markdown, /45,189,256 public event objects/);
  assert.match(markdown, /102,702,850 string-field reads/);
  assert.match(markdown, /throughput `41\.10 MiB\/s`/);
  assert.match(markdown, /`boundedMemory: false`/);
  assert.match(markdown, /peak RSS\s+`13\.45 GiB`/);
  assert.match(markdown, /remaining\s+complete-string problem is memory\/throughput\/materialization/);
  assert.match(markdown, /does not cover Bun\/JSC or browser engines/);
  assert.match(markdown, /not a global runtime-limit\s+proof/);
  assert.match(markdown, /not an allocation profile or machine-code\s+trace/);
  assert.match(markdown, /Rust global allocator counter/);
  assert.match(markdown, /170,824\s+allocation operations/);
  assert.match(markdown, /phase attribution/);
  assert.match(markdown, /`attribute-collection` as the dominant measured allocator phase/);
  assert.match(markdown, /341,635 attribute collection attempts per\s+run, 170,817 non-empty attribute vectors, 284,695 collected attribute items,\s+683,268 total `Vec` capacity slots, and max capacity 4/);
  assert.match(markdown, /not native stack unwinding/);
  assert.match(markdown, /284,695 borrowed and 0 owned/);
  assert.match(markdown, /repeats the same measured-window allocator counter with `runs=3`/);
  assert.match(markdown, /every sample reported 170,824 allocation operations,\s+27\.13 MiB total\s+allocated bytes, 0 B net allocation/);
  assert.match(markdown, /Summed across the three runs,\s+`attribute-collection` still dominated with 512,451 allocation operations/);
  assert.match(markdown, /`cdata-utf8` 33,462 \/ 0/);
  assert.match(markdown, /escaped-utf8/);
  assert.match(markdown, /nonascii-utf8/);
  assert.match(markdown, /cdata-utf8/);
  assert.match(markdown, /utf8-bom/);
  assert.match(markdown, /quick-xml encoding surface audit records[\s\S]*comparator has no active `encoding` feature and rejects a UTF-16\s+probe/);
  assert.match(markdown, /does\s+not prove a\s+JavaScript runtime ceiling/);
  assert.match(markdown, /sampled JFR allocation stacks/);
  assert.match(markdown, /comparator-local accessor shape counters/);
  assert.match(markdown, /284,695 attribute value reads/);
  assert.match(markdown, /TextBuffer\.contentsAsString/);
  assert.match(markdown, /repeats the measured-window JFR capture/);
  assert.match(markdown, /again emitted 10 measured-window allocation samples/);
  assert.match(markdown, /with 9\s+string-boundary samples/);
  assert.match(markdown, /7 were string-boundary samples/);
  assert.match(markdown, /second measured-window sample\s+with the same allocation-path shape/);
  assert.match(markdown, /after warmups and after the pre-run\s+`System\.gc\(\)`/);
  assert.match(markdown, /It is allocation-path evidence, not a\s+speed baseline/);
  assert.match(markdown, /not a deterministic allocation census/);
  assert.match(markdown, /comparator-local attribute `Vec` shape directly/);
  assert.match(markdown, /Bun\/JSC Heap Allocation Profile/);
  assert.match(markdown, /retained heap snapshot pass/);
  assert.match(markdown, /`rawFrameNameId` reported\s+`75\.37 MiB\/s`, `191\.73 MiB` max RSS, 2,198,945 retained heap bytes/);
  assert.match(markdown, /not a 1 GiB profile row, not Bun\/JSC codegen\/IR evidence/);
});
