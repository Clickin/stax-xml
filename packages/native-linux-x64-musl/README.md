# @stax-xml/native-linux-x64-musl

Linux x64 musl native acceleration package for `stax-xml`.

This package is installed as an optional dependency by `stax-xml`; import
`stax-xml/runtime` to resolve it with wasm and JavaScript fallback.
